// Thin re-export so consumers use `@/hooks/useTheme` consistently.
// ThemeContext is not exported from ThemeProvider — use useTheme() hook instead.
export { useTheme } from "@/components/providers/ThemeProvider"
