---
phase: 01-auth-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/lib/axios/index.ts
  - website/src/lib/axios/admin-client.ts
  - website/src/lib/axios/customer-client.ts
  - website/src/lib/axios/agent-client.ts
  - website/src/lib/schemas/auth.ts
  - website/src/stores/auth-store.ts
  - website/src/stores/admin-auth-slice.ts
  - website/src/stores/customer-auth-slice.ts
  - website/src/stores/agent-auth-slice.ts
  - website/src/api/auth.ts
  - website/src/api/types.ts
  - website/src/lib/utils.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-08

must_haves:
  truths:
    - "Zustand store persists admin tokens (access+refresh), customer token, and agent token across browser refreshes"
    - "Admin Axios client attaches Bearer token and silently refreshes on 401 using refresh token"
    - "Customer and agent Axios clients attach Bearer token and redirect to login on 401 (no refresh)"
    - "All schemas match OpenAPI exactly: admin login returns access_token+refresh_token+user, refresh returns token+refresh_token, portals return single token+entity"
  artifacts:
    - path: "website/src/stores/auth-store.ts"
      provides: "Zustand store with persist middleware, three auth slices, hydration gate"
      contains: "partialize"
    - path: "website/src/lib/schemas/auth.ts"
      provides: "Zod schemas matching OpenAPI exactly"
      contains: "PortalLoginFormSchema"
    - path: "website/src/lib/axios/admin-client.ts"
      provides: "Admin Axios client with token refresh interceptor"
      contains: "data.data.token"
    - path: "website/src/api/auth.ts"
      provides: "Raw API call functions for all three portals"
      contains: "identifier"
    - path: "website/src/api/types.ts"
      provides: "TypeScript types derived from Zod schemas"
      contains: "export type"
  key_links:
    - from: "website/src/lib/axios/admin-client.ts"
      to: "website/src/stores/auth-store.ts"
      via: "useAuthStore.getState() vanilla getters for token and setTokens"
      pattern: "useAuthStore\\.getState"
    - from: "website/src/api/auth.ts"
      to: "website/src/lib/schemas/auth.ts"
      via: "Schema.parse on responses"
      pattern: "Schema\\.parse"
    - from: "website/src/lib/axios/customer-client.ts"
      to: "website/src/stores/auth-store.ts"
      via: "useAuthStore.getState() for clearAuth on 401"
      pattern: "useAuthStore\\.getState"
---

<objective>
Build the complete auth data layer: Zustand store with three portal slices and token persistence, Zod schemas matching OpenAPI exactly, three Axios clients with appropriate interceptors, and raw API functions for all auth endpoints.

Purpose: The data layer is the foundation for all auth UI. It must correctly parse OpenAPI response shapes (admin has access_token+refresh_token, portals have single token), handle token refresh for admin, and persist auth state across browser refreshes. The existing `website/src/stores/auth-store.ts` uses cookies and mock data -- this replaces it entirely with localStorage-based persistence and real API integration.

Output: Working auth data layer with correct schemas, API functions, token refresh, and state persistence for all three portals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01/01-CONTEXT.md
@.planning/phases/01/01-UI-SPEC.md
@docs/openapi.docs.yml

<interfaces>
<!-- Key OpenAPI schemas the executor MUST match exactly -->

OpenAPI LoginResponse (line 215):
  access_token: string, refresh_token: string, user: UserResponse

OpenAPI RefreshTokenResponse (line 2522-2524):
  token: string, refresh_token: string

OpenAPI PortalLoginResponse (line 2374):
  token: string, customer: CustomerResponse

OpenAPI AgentPortalLoginResponse (line 2411):
  token: string, agent: SalesAgentResponse

OpenAPI UserResponse (line 260):
  id: uuid, full_name: string, email: string, phone: string, role: enum[superadmin,admin,cs,billing,technician,readonly], is_active: boolean, last_login: date-time|null, created_at: date-time, updated_at: date-time

OpenAPI CustomerResponse (line 352):
  id: uuid, customer_code: string, full_name: string, username: string, email: string, phone: string, id_card_number: string, address: string, latitude: double, longitude: double, is_active: boolean, notes: string

OpenAPI SalesAgentResponse (line 1168):
  id: uuid, router_id: uuid, name: string, phone: string, username: string, status: enum[active,inactive], voucher_mode: enum[mix,num,alp], voucher_length: int, voucher_type: enum[upp,up], bill_discount: double, billing_cycle: enum[weekly,monthly], billing_day: int, created_at: date-time, updated_at: date-time

OpenAPI ChangePasswordRequest (line 232):
  old_password: string (required), new_password: string (required, minLength 8)

All responses wrapped in: { success: boolean, data: T }
Error responses: { success: false, error: string }

Existing RouterContext (from website/src/routes/__root.tsx):
  Currently only has { queryClient: QueryClient }
  Will be extended in Plan 04 to include auth context.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zod schemas, API types, and Zustand auth store with three slices</name>
  <files>
    website/src/lib/schemas/auth.ts
    website/src/api/types.ts
    website/src/stores/auth-store.ts
  </files>
  <read_first>
    @docs/openapi.docs.yml (lines 161-240, 260-286, 352-381, 1168-1207, 2364-2417)
    @website/src/stores/auth-store.ts (existing to understand what to replace)
    @website/src/lib/utils.ts
  </read_first>
  <action>
    1. Create `website/src/lib/schemas/auth.ts`:

       This is a NEW file. Define Zod schemas matching OpenAPI exactly:

       ```typescript
       import { z } from 'zod'

       // Generic API response wrapper
       export const ApiResponseSchema = <T extends z.ZodTypeAny>(dataSchema: T) =>
         z.object({
           success: z.boolean(),
           data: dataSchema,
         })

       // Error response
       export const ErrorResponseSchema = z.object({
         success: z.literal(false),
         error: z.string(),
       })

       // Admin login
       export const AdminUserSchema = z.object({
         id: z.string(),
         full_name: z.string(),
         email: z.string().email(),
         phone: z.string(),
         role: z.enum(['superadmin', 'admin', 'cs', 'billing', 'technician', 'readonly']),
         is_active: z.boolean(),
         last_login: z.string().nullable(),
         created_at: z.string(),
         updated_at: z.string(),
       })

       export const AdminLoginDataSchema = z.object({
         access_token: z.string(),
         refresh_token: z.string(),
         user: AdminUserSchema,
       })

       export const AdminLoginResponseSchema = ApiResponseSchema(AdminLoginDataSchema)

       // Admin refresh -- NOTE: returns `token` NOT `access_token`
       export const AdminRefreshDataSchema = z.object({
         token: z.string(),
         refresh_token: z.string(),
       })

       export const AdminRefreshResponseSchema = ApiResponseSchema(AdminRefreshDataSchema)

       // Admin form schemas
       export const LoginFormSchema = z.object({
         email: z.string().min(1, 'Email wajib diisi').email('Email tidak valid'),
         password: z.string().min(1, 'Password wajib diisi'),
       })

       // Change password
       export const ChangePasswordSchema = z.object({
         old_password: z.string().min(1, 'Password lama wajib diisi'),
         new_password: z.string().min(8, 'Password baru minimal 8 karakter'),
         confirm_password: z.string().min(1, 'Konfirmasi password wajib diisi'),
       }).refine((data) => data.new_password === data.confirm_password, {
         message: 'Password baru dan konfirmasi tidak cocok',
         path: ['confirm_password'],
       })

       // Message response
       export const MessageResponseSchema = z.object({
         success: z.boolean(),
         data: z.object({ message: z.string() }),
       })

       // Customer portal login
       export const CustomerUserSchema = z.object({
         id: z.string(),
         customer_code: z.string(),
         full_name: z.string(),
         username: z.string(),
         email: z.string().email(),
         phone: z.string(),
       })

       export const PortalLoginDataSchema = z.object({
         token: z.string(),
         customer: CustomerUserSchema,
       })

       export const PortalLoginResponseSchema = ApiResponseSchema(PortalLoginDataSchema)

       export const PortalLoginFormSchema = z.object({
         identifier: z.string().min(1, 'Identifier wajib diisi'),
         password: z.string().min(1, 'Password wajib diisi'),
       })

       // Agent portal login
       export const AgentUserSchema = z.object({
         id: z.string(),
         name: z.string(),
         phone: z.string(),
         username: z.string(),
         status: z.enum(['active', 'inactive']),
       })

       export const AgentLoginDataSchema = z.object({
         token: z.string(),
         agent: AgentUserSchema,
       })

       export const AgentLoginResponseSchema = ApiResponseSchema(AgentLoginDataSchema)

       export const AgentLoginFormSchema = z.object({
         username: z.string().min(1, 'Username wajib diisi'),
         password: z.string().min(1, 'Password wajib diisi'),
       })
       ```

    2. Create `website/src/api/types.ts`:

       This is a NEW file (directory may not exist yet -- create `website/src/api/`):

       ```typescript
       import type { z } from 'zod'
       import type {
         AdminLoginDataSchema,
         AdminRefreshDataSchema,
         PortalLoginDataSchema,
         AgentLoginDataSchema,
         AdminUserSchema,
         CustomerUserSchema,
         AgentUserSchema,
         LoginFormSchema,
         PortalLoginFormSchema,
         AgentLoginFormSchema,
         ChangePasswordSchema,
       } from '@/lib/schemas/auth'

       export type AdminUser = z.infer<typeof AdminUserSchema>
       export type CustomerUser = z.infer<typeof CustomerUserSchema>
       export type AgentUser = z.infer<typeof AgentUserSchema>

       export type AdminLoginData = z.infer<typeof AdminLoginDataSchema>
       export type AdminRefreshData = z.infer<typeof AdminRefreshDataSchema>
       export type PortalLoginData = z.infer<typeof PortalLoginDataSchema>
       export type AgentLoginData = z.infer<typeof AgentLoginDataSchema>

       export type LoginFormValues = z.infer<typeof LoginFormSchema>
       export type PortalLoginFormValues = z.infer<typeof PortalLoginFormSchema>
       export type AgentLoginFormValues = z.infer<typeof AgentLoginFormSchema>
       export type ChangePasswordValues = z.infer<typeof ChangePasswordSchema>

       export type ApiResponse<T> = {
         success: boolean
         data: T
       }
       ```

    3. Rewrite `website/src/stores/auth-store.ts`:

       Replace the existing cookie-based mock auth store entirely with a three-slice Zustand store using `zustand/middleware/persist`:

       ```typescript
       import { create } from 'zustand'
       import { persist } from 'zustand/middleware'
       import type { AdminUser, CustomerUser, AgentUser } from '@/api/types'

       // --- Admin Auth Slice ---
       interface AdminAuthSlice {
         adminAccessToken: string | null
         adminRefreshToken: string | null
         adminUser: AdminUser | null
         adminIsAuthenticated: boolean
         adminSetTokens: (accessToken: string, refreshToken: string) => void
         adminSetUser: (user: AdminUser) => void
         adminClearAuth: () => void
       }

       // --- Customer Auth Slice ---
       interface CustomerAuthSlice {
         customerToken: string | null
         customerUser: CustomerUser | null
         customerIsAuthenticated: boolean
         customerSetToken: (token: string) => void
         customerSetUser: (user: CustomerUser) => void
         customerClearAuth: () => void
       }

       // --- Agent Auth Slice ---
       interface AgentAuthSlice {
         agentToken: string | null
         agentUser: AgentUser | null
         agentIsAuthenticated: boolean
         agentSetToken: (token: string) => void
         agentSetUser: (user: AgentUser) => void
         agentClearAuth: () => void
       }

       // --- Hydration Slice ---
       interface HydrationSlice {
         isHydrated: boolean
         setHydrated: () => void
       }

       type AuthStoreState = AdminAuthSlice & CustomerAuthSlice & AgentAuthSlice & HydrationSlice

       export const useAuthStore = create<AuthStoreState>()(
         persist(
           (set) => ({
             // Admin
             adminAccessToken: null,
             adminRefreshToken: null,
             adminUser: null,
             adminIsAuthenticated: false,
             adminSetTokens: (accessToken, refreshToken) =>
               set((state) => ({
                 ...state,
                 adminAccessToken: accessToken,
                 adminRefreshToken: refreshToken,
                 adminIsAuthenticated: true,
               })),
             adminSetUser: (user) =>
               set((state) => ({ ...state, adminUser: user })),
             adminClearAuth: () =>
               set((state) => ({
                 ...state,
                 adminAccessToken: null,
                 adminRefreshToken: null,
                 adminUser: null,
                 adminIsAuthenticated: false,
               })),

             // Customer
             customerToken: null,
             customerUser: null,
             customerIsAuthenticated: false,
             customerSetToken: (token) =>
               set((state) => ({
                 ...state,
                 customerToken: token,
                 customerIsAuthenticated: true,
               })),
             customerSetUser: (user) =>
               set((state) => ({ ...state, customerUser: user })),
             customerClearAuth: () =>
               set((state) => ({
                 ...state,
                 customerToken: null,
                 customerUser: null,
                 customerIsAuthenticated: false,
               })),

             // Agent
             agentToken: null,
             agentUser: null,
             agentIsAuthenticated: false,
             agentSetToken: (token) =>
               set((state) => ({
                 ...state,
                 agentToken: token,
                 agentIsAuthenticated: true,
               })),
             agentSetUser: (user) =>
               set((state) => ({ ...state, agentUser: user })),
             agentClearAuth: () =>
               set((state) => ({
                 ...state,
                 agentToken: null,
                 agentUser: null,
                 agentIsAuthenticated: false,
               })),

             // Hydration
             isHydrated: false,
             setHydrated: () => set((state) => ({ ...state, isHydrated: true })),
           }),
           {
             name: 'mikmongo-auth',
             partialize: (state) => ({
               adminAccessToken: state.adminAccessToken,
               adminRefreshToken: state.adminRefreshToken,
               adminUser: state.adminUser,
               adminIsAuthenticated: state.adminIsAuthenticated,
               customerToken: state.customerToken,
               customerUser: state.customerUser,
               customerIsAuthenticated: state.customerIsAuthenticated,
               agentToken: state.agentToken,
               agentUser: state.agentUser,
               agentIsAuthenticated: state.agentIsAuthenticated,
             }),
             onRehydrateStorage: () => (state) => {
               state?.setHydrated()
             },
           }
         )
       )
       ```

       Key design decisions:
       - Admin has `accessToken` + `refreshToken` (per OpenAPI LoginResponse which returns both)
       - Customer and Agent have single `token` (per OpenAPI PortalLoginResponse/AgentPortalLoginResponse which return single token)
       - `partialize` excludes all actions and `isHydrated` from persistence
       - `onRehydrateStorage` calls `setHydrated` after rehydration completes (hydration gate)
       - Uses `localStorage` via Zustand persist (default storage) -- NOT cookies
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - website/src/lib/schemas/auth.ts contains "PortalLoginFormSchema"
    - website/src/lib/schemas/auth.ts contains "AgentLoginFormSchema"
    - website/src/lib/schemas/auth.ts contains "ChangePasswordSchema"
    - website/src/lib/schemas/auth.ts contains "AdminRefreshDataSchema" with "token: z.string()"
    - website/src/lib/schemas/auth.ts contains "access_token: z.string()" (admin login response)
    - website/src/api/types.ts contains "export type AdminLoginData"
    - website/src/api/types.ts contains "export type PortalLoginData"
    - website/src/api/types.ts contains "export type AgentLoginData"
    - website/src/stores/auth-store.ts contains "partialize"
    - website/src/stores/auth-store.ts contains "adminAccessToken"
    - website/src/stores/auth-store.ts contains "customerToken" (NOT customerAccessToken)
    - website/src/stores/auth-store.ts contains "agentToken" (NOT agentAccessToken)
    - website/src/stores/auth-store.ts contains "setHydrated"
    - website/src/stores/auth-store.ts contains "onRehydrateStorage"
    - website/src/stores/auth-store.ts does NOT contain "thisisjustarandomstring"
    - website/src/stores/auth-store.ts does NOT import from "@/lib/cookies"
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>
    Zod schemas match OpenAPI contract exactly: admin login has access_token+refresh_token+user, admin refresh has token+refresh_token, customer portal has token+customer, agent portal has token+agent. API types file exports all derived types. Zustand store has three slices with persist middleware, partialize for token/user persistence only, and hydration gate via onRehydrateStorage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Axios clients and API auth functions</name>
  <files>
    website/src/lib/axios/admin-client.ts
    website/src/lib/axios/customer-client.ts
    website/src/lib/axios/agent-client.ts
    website/src/api/auth.ts
  </files>
  <read_first>
    @website/src/stores/auth-store.ts (created in Task 1)
    @website/src/lib/schemas/auth.ts (created in Task 1)
    @docs/openapi.docs.yml (lines 2462-2575, 3850-3878, 4094-4122)
  </read_first>
  <action>
    1. Create `website/src/lib/axios/admin-client.ts`:

       ```typescript
       import axios, { type AxiosError, type InternalAxiosRequestConfig } from 'axios'
       import { useAuthStore } from '@/stores/auth-store'

       export const adminClient = axios.create({
         baseURL: '/api/v1',
         headers: { 'Content-Type': 'application/json' },
       })

       // Request interceptor: attach Bearer token
       adminClient.interceptors.request.use((config: InternalAxiosRequestConfig) => {
         const token = useAuthStore.getState().adminAccessToken
         if (token && config.headers) {
           config.headers.Authorization = `Bearer ${token}`
         }
         return config
       })

       // Response interceptor: silent token refresh on 401
       let isRefreshing = false
       let failedQueue: Array<{
         resolve: (token: string) => void
         reject: (error: unknown) => void
       }> = []

       function processQueue(error: unknown, token: string | null = null) {
         failedQueue.forEach((prom) => {
           if (error) {
             prom.reject(error)
           } else {
             prom.resolve(token!)
           }
         })
         failedQueue = []
       }

       adminClient.interceptors.response.use(
         (response) => response,
         async (error: AxiosError) => {
           const originalRequest = error.config as InternalAxiosRequestConfig & { _retry?: boolean }

           if (error.response?.status === 401 && !originalRequest._retry) {
             if (isRefreshing) {
               return new Promise((resolve, reject) => {
                 failedQueue.push({ resolve, reject })
               }).then((token) => {
                 if (originalRequest.headers) {
                   originalRequest.headers.Authorization = `Bearer ${token}`
                 }
                 return adminClient(originalRequest)
               })
             }

             originalRequest._retry = true
             isRefreshing = true

             const refreshToken = useAuthStore.getState().adminRefreshToken
             if (!refreshToken) {
               useAuthStore.getState().adminClearAuth()
               window.location.href = '/sign-in'
               return Promise.reject(error)
             }

             try {
               // Use plain axios to avoid interceptor loop
               const response = await axios.post('/api/v1/auth/refresh', {
                 refresh_token: refreshToken,
               })
               const newAccessToken: string = response.data.data.token
               const newRefreshToken: string = response.data.data.refresh_token

               useAuthStore.getState().adminSetTokens(newAccessToken, newRefreshToken)
               processQueue(null, newAccessToken)

               if (originalRequest.headers) {
                 originalRequest.headers.Authorization = `Bearer ${newAccessToken}`
               }
               return adminClient(originalRequest)
             } catch (refreshError) {
               processQueue(refreshError, null)
               useAuthStore.getState().adminClearAuth()
               window.location.href = '/sign-in'
               return Promise.reject(refreshError)
             } finally {
               isRefreshing = false
             }
           }

           return Promise.reject(error)
         }
       )
       ```

       CRITICAL: The refresh response uses `response.data.data.token` (NOT `response.data.data.access_token`). This matches the OpenAPI spec where refresh returns `{ token, refresh_token }`.

    2. Create `website/src/lib/axios/customer-client.ts`:

       ```typescript
       import axios, { type AxiosError } from 'axios'
       import { useAuthStore } from '@/stores/auth-store'

       export const customerClient = axios.create({
         baseURL: '/portal/v1',
         headers: { 'Content-Type': 'application/json' },
       })

       // Request interceptor: attach Bearer token
       customerClient.interceptors.request.use((config) => {
         const token = useAuthStore.getState().customerToken
         if (token && config.headers) {
           config.headers.Authorization = `Bearer ${token}`
         }
         return config
       })

       // Response interceptor: no refresh (portal uses single token)
       customerClient.interceptors.response.use(
         (response) => response,
         async (error: AxiosError) => {
           if (error.response?.status === 401) {
             useAuthStore.getState().customerClearAuth()
             window.location.href = '/customer/login'
           }
           return Promise.reject(error)
         }
       )
       ```

    3. Create `website/src/lib/axios/agent-client.ts`:

       ```typescript
       import axios, { type AxiosError } from 'axios'
       import { useAuthStore } from '@/stores/auth-store'

       export const agentClient = axios.create({
         baseURL: '/agent-portal/v1',
         headers: { 'Content-Type': 'application/json' },
       })

       // Request interceptor: attach Bearer token
       agentClient.interceptors.request.use((config) => {
         const token = useAuthStore.getState().agentToken
         if (token && config.headers) {
           config.headers.Authorization = `Bearer ${token}`
         }
         return config
       })

       // Response interceptor: no refresh (portal uses single token)
       agentClient.interceptors.response.use(
         (response) => response,
         async (error: AxiosError) => {
           if (error.response?.status === 401) {
             useAuthStore.getState().agentClearAuth()
             window.location.href = '/agent/login'
           }
           return Promise.reject(error)
         }
       )
       ```

    4. Create `website/src/api/auth.ts`:

       ```typescript
       import axios from 'axios'
       import { adminClient } from '@/lib/axios/admin-client'
       import {
         AdminLoginResponseSchema,
         AdminRefreshResponseSchema,
         PortalLoginResponseSchema,
         AgentLoginResponseSchema,
         MessageResponseSchema,
       } from '@/lib/schemas/auth'
       import type {
         AdminLoginData,
         AdminRefreshData,
         PortalLoginData,
         AgentLoginData,
       } from '@/api/types'

       // Admin login -- uses plain axios (no token yet)
       export async function adminLogin(email: string, password: string): Promise<AdminLoginData> {
         const response = await axios.post('/api/v1/auth/login', { email, password })
         const parsed = AdminLoginResponseSchema.parse(response.data)
         return parsed.data
       }

       // Admin refresh -- uses plain axios (to avoid interceptor loop)
       export async function adminRefreshToken(refreshToken: string): Promise<AdminRefreshData> {
         const response = await axios.post('/api/v1/auth/refresh', { refresh_token: refreshToken })
         const parsed = AdminRefreshResponseSchema.parse(response.data)
         return parsed.data
       }

       // Admin change password -- uses adminClient (authenticated)
       export async function adminChangePassword(oldPassword: string, newPassword: string) {
         const response = await adminClient.post('/auth/change-password', {
           old_password: oldPassword,
           new_password: newPassword,
         })
         return MessageResponseSchema.parse(response.data)
       }

       // Admin logout -- uses adminClient (authenticated)
       export async function adminLogout() {
         const response = await adminClient.post('/auth/logout')
         return MessageResponseSchema.parse(response.data)
       }

       // Admin get current user -- uses adminClient (authenticated)
       export async function adminGetMe() {
         const response = await adminClient.get('/auth/me')
         return response.data
       }

       // Customer login -- uses plain axios (no token yet)
       export async function customerLogin(identifier: string, password: string): Promise<PortalLoginData> {
         const response = await axios.post('/portal/v1/login', { identifier, password })
         const parsed = PortalLoginResponseSchema.parse(response.data)
         return parsed.data
       }

       // Agent login -- uses plain axios (no token yet)
       export async function agentLogin(username: string, password: string): Promise<AgentLoginData> {
         const response = await axios.post('/agent-portal/v1/login', { username, password })
         const parsed = AgentLoginResponseSchema.parse(response.data)
         return parsed.data
       }
       ```

       Key: Login functions use plain `axios` (not the interceptored clients) because there is no token yet. Change password and logout use `adminClient` because they require authentication.

    5. Remove the old `website/src/lib/cookies.ts` file is NOT needed. Leave it in place for now since the sidebar state cookie uses it. The auth store no longer uses cookies.

    6. Also add a Vite proxy configuration. Update `website/vite.config.ts` to add proxy for API routes. But wait -- the backend runs on port 8080 per .env. However, this is an environment-specific concern. The Axios clients use relative URLs (`/api/v1`, `/portal/v1`, `/agent-portal/v1`) which means the backend must be served at the same origin or proxied. For development, add a proxy:

       Read `website/vite.config.ts` first, then add:
       ```typescript
       server: {
         proxy: {
           '/api': 'http://localhost:8080',
           '/portal': 'http://localhost:8080',
           '/agent-portal': 'http://localhost:8080',
         },
       },
       ```
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - website/src/lib/axios/admin-client.ts contains "data.data.token" (refresh response parsing)
    - website/src/lib/axios/admin-client.ts contains "isRefreshing" (refresh queue logic)
    - website/src/lib/axios/admin-client.ts contains "failedQueue" (retry queue)
    - website/src/lib/axios/customer-client.ts does NOT contain "isRefreshing" (no refresh)
    - website/src/lib/axios/agent-client.ts does NOT contain "isRefreshing" (no refresh)
    - website/src/lib/axios/customer-client.ts contains "customerClearAuth()"
    - website/src/lib/axios/agent-client.ts contains "agentClearAuth()"
    - website/src/api/auth.ts contains "identifier" (customer login field)
    - website/src/api/auth.ts contains "username" (agent login field, NOT email)
    - website/src/api/auth.ts contains "adminChangePassword"
    - website/src/api/auth.ts contains "adminLogout"
    - website/src/api/auth.ts imports from "@/lib/axios/admin-client"
    - website/vite.config.ts contains "proxy"
    - website/vite.config.ts contains "/api" and "/portal" and "/agent-portal"
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>
    Three Axios clients created: admin with silent token refresh on 401 (parses `token` field from refresh response), customer and agent with simple 401 redirect (no refresh, single token). API auth functions use plain axios for login (no token yet) and adminClient for authenticated endpoints. Vite dev proxy configured for all three API prefixes.
  </done>
</task>

</tasks>

<verification>
1. `cd website && npx tsc --noEmit` -- TypeScript compilation passes with zero errors
2. `grep "data.data.token" website/src/lib/axios/admin-client.ts` -- refresh uses `token` not `access_token`
3. `grep "identifier" website/src/api/auth.ts` -- customer login sends identifier
4. `grep "username" website/src/api/auth.ts` -- agent login sends username
5. `grep "partialize" website/src/stores/auth-store.ts` -- persist middleware configured
6. `grep "customerToken" website/src/stores/auth-store.ts` -- customer uses single token
7. `grep "onRehydrateStorage" website/src/stores/auth-store.ts` -- hydration gate present
</verification>

<success_criteria>
- Zod schemas parse OpenAPI response shapes exactly
- Admin refresh response uses `token` field (NOT `access_token`)
- Customer/agent portal responses parse single token + entity
- Zustand store persists tokens for all three portals
- Admin Axios client has silent refresh with retry queue
- Customer/agent Axios clients have 401 redirect (no refresh)
- API functions send correct request fields
- TypeScript compiles with zero errors
- Vite proxy configured for development
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-01-SUMMARY.md`
</output>
