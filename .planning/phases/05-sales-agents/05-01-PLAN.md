---
phase: 05-sales-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/lib/schemas/sales.ts
  - website/src/lib/schemas/mikhmon.ts
  - website/src/api/sales-agent.ts
  - website/src/api/agent-invoice.ts
  - website/src/api/hotspot-sale.ts
  - website/src/api/mikhmon.ts
  - website/src/api/portal/agent-sales.ts
  - website/src/hooks/use-sales-agents.ts
  - website/src/hooks/use-agent-invoices.ts
  - website/src/hooks/use-hotspot-sales.ts
  - website/src/hooks/use-mikhmon.ts
  - website/src/hooks/use-portal-sales.ts
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05, AGNT-06, AGNT-07, AGNT-08, HS-01, HS-02, HS-03, HS-04, HS-05, HS-06]
must_haves:
  truths:
    - "Zod schemas exist for SalesAgent, SalesProfilePrice, AgentInvoice, HotspotSale, Voucher, VoucherBatch, MikhmonProfile, MikhmonReportSummary, ExpireStatus"
    - "API functions exist for all 20+ Phase 5 endpoints (sales-agents, agent-invoices, hotspot-sales, mikhmon/*, agent-portal/v1/sales)"
    - "TanStack Query hooks exist with invalidation keys for all resources"
  artifacts:
    - path: "website/src/lib/schemas/sales.ts"
      provides: "Sales agent / agent invoice / hotspot sale schemas"
      contains: "salesAgentResponseSchema"
    - path: "website/src/lib/schemas/mikhmon.ts"
      provides: "Mikhmon voucher / profile / report schemas"
      contains: "voucherBatchResponseSchema"
    - path: "website/src/api/sales-agent.ts"
      provides: "listSalesAgents, createSalesAgent, updateSalesAgent, listAgentProfilePrices, upsertAgentProfilePrice"
    - path: "website/src/api/agent-invoice.ts"
      provides: "listAgentInvoices, getAgentInvoice, generateAgentInvoice, markAgentInvoicePaid, cancelAgentInvoice, processScheduledAgentInvoices"
    - path: "website/src/api/hotspot-sale.ts"
      provides: "listHotspotSales, listHotspotSalesByRouter"
    - path: "website/src/api/mikhmon.ts"
      provides: "generateVouchers, getVouchers, removeVoucherBatch, createMikhmonProfile, updateMikhmonProfile, generateMikhmonScript, getMikhmonReports, getMikhmonReportSummary, setupExpireMonitor, disableExpireMonitor, getExpireMonitorStatus, generateExpireScript"
    - path: "website/src/api/portal/agent-sales.ts"
      provides: "listAgentPortalSales"
    - path: "website/src/hooks/use-sales-agents.ts"
      provides: "useSalesAgents, useCreateSalesAgent, useUpdateSalesAgent, useAgentProfilePrices, useUpsertAgentProfilePrice"
    - path: "website/src/hooks/use-agent-invoices.ts"
      provides: "useAgentInvoices, useAgentInvoice, useGenerateAgentInvoice, useMarkAgentInvoicePaid, useCancelAgentInvoice, useProcessScheduledAgentInvoices"
    - path: "website/src/hooks/use-hotspot-sales.ts"
      provides: "useHotspotSales"
    - path: "website/src/hooks/use-mikhmon.ts"
      provides: "useMikhmonVouchers, useGenerateVouchers, useRemoveVoucherBatch, useCreateMikhmonProfile, useUpdateMikhmonProfile, useGenerateMikhmonScript, useMikhmonReports, useMikhmonReportSummary, useSetupExpireMonitor, useDisableExpireMonitor, useExpireMonitorStatus, useGenerateExpireScript"
    - path: "website/src/hooks/use-portal-sales.ts"
      provides: "useAgentPortalSales"
  key_links:
    - from: "website/src/api/sales-agent.ts"
      to: "adminClient"
      via: "import from @/lib/axios/admin-client"
      pattern: "adminClient\\.(get|post|put|delete)"
    - from: "website/src/api/portal/agent-sales.ts"
      to: "agentClient"
      via: "import from @/lib/axios/agent-client"
      pattern: "agentClient\\.get"
---

<objective>
Build the Phase 5 data layer: Zod schemas, Axios API functions, and TanStack Query hooks for sales agents, agent invoices, hotspot sales, Mikhmon (vouchers/profiles/reports/expire), and agent portal sales history. This is the Wave 1 prerequisite for all Phase 5 UI plans.

Purpose: Provide a type-safe, reusable data layer so downstream UI plans (05-02..05-06) can simply import hooks and render.
Output: Schemas, API functions, hooks matching OpenAPI contract for all 14 Phase 5 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sales-agents/05-CONTEXT.md
@docs/openapi.docs.yml
@website/src/lib/schemas/billing.ts
@website/src/api/invoice.ts
@website/src/api/portal/agent-invoice.ts
@website/src/hooks/use-invoices.ts
@website/src/hooks/use-portal-billing.ts

<interfaces>
Established patterns from Phase 4 (MUST follow):

**Schema pattern** (website/src/lib/schemas/billing.ts):
- Use `z.object({...})` with `z.string().uuid()`, `z.enum([...])`, `z.number()`, `z.string().nullable()`
- Export inferred type: `export type XxxResponse = z.infer<typeof xxxResponseSchema>`
- List response wrapper: `ApiResponseSchema(z.array(xxxResponseSchema))` with optional `MetaSchema`
- Detail response wrapper: `ApiResponseSchema(xxxResponseSchema)`

**API function pattern** (website/src/api/invoice.ts):
```typescript
import { adminClient } from '@/lib/axios/admin-client'
export async function listX(limit?: number, offset?: number): Promise<{items: X[], meta: {...}}> {
  const params: Record<string, number | string> = {}
  if (limit !== undefined) params.limit = limit
  if (offset !== undefined) params.offset = offset
  const response = await adminClient.get('/path', { params })
  const parsed = XListResponseSchema.parse(response.data)
  return { items: parsed.data, meta: parsed.meta }
}
```

**Hook pattern** (website/src/hooks/use-invoices.ts):
```typescript
export function useX(params) {
  return useQuery({
    queryKey: ['x', ...params],
    queryFn: () => listX(params),
    staleTime: 2 * 60 * 1000,
  })
}

export function useCreateX() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (data) => createX(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['x'] })
      toast.success('X berhasil dibuat')
    },
    onError: (err: unknown) => {
      const message = (err as { response?: { data?: { error?: string } } })?.response?.data?.error ?? 'Gagal'
      toast.error(message)
    },
  })
}
```

**Indonesian toast messages** (use these):
- success create: "{Entity} berhasil dibuat"
- success update: "{Entity} berhasil diperbarui"
- success delete: "{Entity} berhasil dihapus"
- error fallback: "Gagal {action} {entity}"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create sales and mikhmon Zod schemas</name>
  <files>website/src/lib/schemas/sales.ts, website/src/lib/schemas/mikhmon.ts</files>
  <read_first>
    - docs/openapi.docs.yml (lines 1099-1364 for sales schemas; lines 2144-2290 for mikhmon schemas)
    - website/src/lib/schemas/billing.ts (pattern to follow for schema shape, ApiResponseSchema wrapper, MetaSchema import)
    - website/src/lib/schemas/auth.ts (for ApiResponseSchema import)
    - website/src/lib/schemas/router.ts (for MetaSchema import)
  </read_first>
  <action>
Create `website/src/lib/schemas/sales.ts` with EXACT Zod schemas matching OpenAPI:

```typescript
import { z } from 'zod'
import { ApiResponseSchema } from '@/lib/schemas/auth'
import { MetaSchema } from '@/lib/schemas/router'

// Sales Agent
export const salesAgentResponseSchema = z.object({
  id: z.string().uuid(),
  router_id: z.string().uuid(),
  name: z.string(),
  phone: z.string().nullable().optional(),
  username: z.string(),
  status: z.enum(['active', 'inactive']),
  voucher_mode: z.enum(['mix', 'num', 'alp']).nullable().optional(),
  voucher_length: z.number().int().nullable().optional(),
  voucher_type: z.enum(['upp', 'up']).nullable().optional(),
  bill_discount: z.number().nullable().optional(),
  billing_cycle: z.enum(['weekly', 'monthly']).nullable().optional(),
  billing_day: z.number().int().nullable().optional(),
  created_at: z.string(),
  updated_at: z.string(),
})
export type SalesAgentResponse = z.infer<typeof salesAgentResponseSchema>

export const createSalesAgentSchema = z.object({
  router_id: z.string().uuid(),
  name: z.string().min(1),
  phone: z.string().optional(),
  username: z.string().min(1),
  password: z.string().min(6),
  status: z.enum(['active', 'inactive']).optional(),
  voucher_mode: z.enum(['mix', 'num', 'alp']).optional(),
  voucher_length: z.number().int().optional(),
  voucher_type: z.enum(['upp', 'up']).optional(),
  bill_discount: z.number().optional(),
  billing_cycle: z.enum(['weekly', 'monthly']).optional(),
  billing_day: z.number().int().min(1).max(31).optional(),
})
export type CreateSalesAgentRequest = z.infer<typeof createSalesAgentSchema>

export const updateSalesAgentSchema = createSalesAgentSchema.partial().omit({ router_id: true, username: true })
export type UpdateSalesAgentRequest = z.infer<typeof updateSalesAgentSchema>

// Profile Price
export const salesProfilePriceResponseSchema = z.object({
  id: z.string().uuid(),
  sales_agent_id: z.string().uuid(),
  profile_name: z.string(),
  base_price: z.number(),
  selling_price: z.number(),
  voucher_length: z.number().int().nullable().optional(),
  is_active: z.boolean(),
  created_at: z.string(),
})
export type SalesProfilePriceResponse = z.infer<typeof salesProfilePriceResponseSchema>

export const upsertProfilePriceSchema = z.object({
  base_price: z.number().min(0),
  selling_price: z.number().min(0),
  voucher_length: z.number().int().optional(),
  is_active: z.boolean().optional(),
})
export type UpsertProfilePriceRequest = z.infer<typeof upsertProfilePriceSchema>

// Agent Invoice
export const agentInvoiceResponseSchema = z.object({
  id: z.string().uuid(),
  agent_id: z.string().uuid(),
  router_id: z.string().uuid(),
  invoice_number: z.string(),
  billing_cycle: z.enum(['weekly', 'monthly']),
  period_start: z.string(),
  period_end: z.string(),
  billing_month: z.number().int().nullable().optional(),
  billing_week: z.number().int().nullable().optional(),
  billing_year: z.number().int(),
  voucher_count: z.number().int(),
  subtotal: z.number(),
  selling_total: z.number(),
  profit: z.number(),
  discount_amount: z.number(),
  total_amount: z.number(),
  paid_amount: z.number(),
  balance: z.number(),
  status: z.enum(['draft', 'unpaid', 'paid', 'cancelled']),
  notes: z.string().nullable().optional(),
  created_at: z.string(),
  updated_at: z.string(),
})
export type AgentInvoiceResponse = z.infer<typeof agentInvoiceResponseSchema>

export const generateAgentInvoiceSchema = z.object({
  period_start: z.string(), // YYYY-MM-DD
  period_end: z.string(),   // YYYY-MM-DD
})
export type GenerateAgentInvoiceRequest = z.infer<typeof generateAgentInvoiceSchema>

export const markAgentPaidSchema = z.object({
  paid_amount: z.number().min(0),
})
export type MarkAgentPaidRequest = z.infer<typeof markAgentPaidSchema>

// Hotspot Sale
export const hotspotSaleResponseSchema = z.object({
  id: z.string().uuid(),
  router_id: z.string().uuid(),
  username: z.string(),
  profile: z.string(),
  price: z.number(),
  selling_price: z.number(),
  prefix: z.string().nullable().optional(),
  batch_code: z.string().nullable().optional(),
  sales_agent_id: z.string().uuid().nullable().optional(),
  created_at: z.string(),
})
export type HotspotSaleResponse = z.infer<typeof hotspotSaleResponseSchema>

// List wrappers
export const SalesAgentListResponseSchema = z.object({ success: z.boolean(), data: z.array(salesAgentResponseSchema), meta: MetaSchema.optional() })
export const SalesAgentDetailResponseSchema = ApiResponseSchema(salesAgentResponseSchema)
export const ProfilePriceListResponseSchema = ApiResponseSchema(z.array(salesProfilePriceResponseSchema))
export const ProfilePriceDetailResponseSchema = ApiResponseSchema(salesProfilePriceResponseSchema)
export const AgentInvoiceListResponseSchema = z.object({ success: z.boolean(), data: z.array(agentInvoiceResponseSchema), meta: MetaSchema.optional() })
export const AgentInvoiceDetailResponseSchema = ApiResponseSchema(agentInvoiceResponseSchema)
export const HotspotSaleListResponseSchema = z.object({ success: z.boolean(), data: z.array(hotspotSaleResponseSchema), meta: MetaSchema.optional() })
```

Then create `website/src/lib/schemas/mikhmon.ts`:

```typescript
import { z } from 'zod'
import { ApiResponseSchema } from '@/lib/schemas/auth'

// Voucher
export const voucherResponseSchema = z.object({
  id: z.string().nullable().optional(),
  name: z.string(),
  password: z.string().nullable().optional(),
  profile: z.string().nullable().optional(),
  server: z.string().nullable().optional(),
  comment: z.string().nullable().optional(),
  code: z.string().nullable().optional(),
  mode: z.string().nullable().optional(),
  date: z.string().nullable().optional(),
})
export type VoucherResponse = z.infer<typeof voucherResponseSchema>

export const voucherBatchResponseSchema = z.object({
  code: z.string(),
  quantity: z.number().int(),
  profile: z.string(),
  server: z.string().nullable().optional(),
  time_limit: z.string().nullable().optional(),
  data_limit: z.string().nullable().optional(),
  vouchers: z.array(voucherResponseSchema),
})
export type VoucherBatchResponse = z.infer<typeof voucherBatchResponseSchema>

export const generateVoucherSchema = z.object({
  quantity: z.number().int().min(1).max(1000),
  server: z.string().optional(),
  profile: z.string().min(1),
  mode: z.enum(['vc', 'up']),
  name_length: z.number().int().min(3).max(12).optional(),
  prefix: z.string().optional(),
  char_set: z.string().min(1),
  time_limit: z.string().optional(),
  data_limit: z.string().optional(),
  comment: z.string().optional(),
})
export type GenerateVoucherRequest = z.infer<typeof generateVoucherSchema>

// Mikhmon Profile
export const mikhmonProfileConfigSchema = z.object({
  name: z.string().nullable().optional(),
  address_pool: z.string().nullable().optional(),
  rate_limit: z.string().nullable().optional(),
  shared_users: z.number().int().nullable().optional(),
  parent_queue: z.string().nullable().optional(),
  price: z.number().int().nullable().optional(),
  selling_price: z.number().int().nullable().optional(),
  validity: z.string().nullable().optional(),
  expire_mode: z.string().nullable().optional(),
  lock_user: z.boolean().nullable().optional(),
  lock_server: z.boolean().nullable().optional(),
  on_login_script: z.string().nullable().optional(),
}).partial()

export const createMikhmonProfileSchema = z.object({
  name: z.string().min(1),
  address_pool: z.string().optional(),
  rate_limit: z.string().optional(),
  shared_users: z.number().int().optional(),
  parent_queue: z.string().optional(),
  config: mikhmonProfileConfigSchema.optional(),
})
export type CreateMikhmonProfileRequest = z.infer<typeof createMikhmonProfileSchema>

export const updateMikhmonProfileSchema = z.object({
  name: z.string().optional(),
  address_pool: z.string().optional(),
  rate_limit: z.string().optional(),
  shared_users: z.number().int().optional(),
  parent_queue: z.string().optional(),
  price: z.number().int().optional(),
  selling_price: z.number().int().optional(),
  validity: z.string().optional(),
  expire_mode: z.string().optional(),
  lock_user: z.boolean().optional(),
  lock_server: z.boolean().optional(),
})
export type UpdateMikhmonProfileRequest = z.infer<typeof updateMikhmonProfileSchema>

export const generateScriptSchema = z.object({
  mode: z.string().min(1),
  profile_name: z.string().min(1),
})
export type GenerateScriptRequest = z.infer<typeof generateScriptSchema>

// Expire
export const expireStatusResponseSchema = z.object({
  enabled: z.boolean(),
  scheduler_name: z.string().nullable().optional(),
  interval: z.string().nullable().optional(),
  last_run: z.string().nullable().optional(),
}).passthrough()
export type ExpireStatusResponse = z.infer<typeof expireStatusResponseSchema>

// Report Summary (flexible — backend returns aggregate object)
export const mikhmonReportSummarySchema = z.object({
  total_sales: z.number().nullable().optional(),
  total_revenue: z.number().nullable().optional(),
  total_vouchers: z.number().int().nullable().optional(),
}).passthrough()
export type MikhmonReportSummaryResponse = z.infer<typeof mikhmonReportSummarySchema>

// Wrappers
export const VoucherBatchDetailResponseSchema = ApiResponseSchema(voucherBatchResponseSchema)
export const VoucherListResponseSchema = ApiResponseSchema(z.array(voucherResponseSchema))
export const ScriptResponseSchema = ApiResponseSchema(z.object({ script: z.string() }))
export const ExpireStatusWrappedSchema = ApiResponseSchema(expireStatusResponseSchema)
export const ExpireToggleResponseSchema = ApiResponseSchema(z.object({ enabled: z.boolean() }))
export const MikhmonReportSummaryWrappedSchema = ApiResponseSchema(mikhmonReportSummarySchema)
export const MikhmonReportsWrappedSchema = z.object({ success: z.boolean(), data: z.unknown() })
```

Use nullable/optional generously on response fields since Go backends often omit empty strings. Use `passthrough()` for dynamic aggregate shapes (Mikhmon report summary).
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/lib/schemas/(sales|mikhmon)\.ts" | head -5; test -z "$(cd website && npx tsc --noEmit 2>&1 | grep -E 'src/lib/schemas/(sales|mikhmon)\.ts')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `website/src/lib/schemas/sales.ts` exists and exports `salesAgentResponseSchema`, `createSalesAgentSchema`, `updateSalesAgentSchema`, `salesProfilePriceResponseSchema`, `upsertProfilePriceSchema`, `agentInvoiceResponseSchema`, `generateAgentInvoiceSchema`, `markAgentPaidSchema`, `hotspotSaleResponseSchema` (verify via grep `export const salesAgentResponseSchema` — match count >= 1)
    - File `website/src/lib/schemas/mikhmon.ts` exists and exports `voucherResponseSchema`, `voucherBatchResponseSchema`, `generateVoucherSchema`, `createMikhmonProfileSchema`, `updateMikhmonProfileSchema`, `generateScriptSchema`, `expireStatusResponseSchema`, `mikhmonReportSummarySchema`
    - Type exports present: grep for `export type SalesAgentResponse` and `export type VoucherBatchResponse` returns match
    - `npx tsc --noEmit` produces no errors in these two files
  </acceptance_criteria>
  <done>Both schema files exist, all exports present, no TS errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create API functions for sales, agent-invoice, hotspot-sale, mikhmon, portal sales</name>
  <files>website/src/api/sales-agent.ts, website/src/api/agent-invoice.ts, website/src/api/hotspot-sale.ts, website/src/api/mikhmon.ts, website/src/api/portal/agent-sales.ts</files>
  <read_first>
    - website/src/api/invoice.ts (pattern for adminClient.get/post + parse)
    - website/src/api/portal/agent-invoice.ts (pattern for agentClient portal API)
    - website/src/lib/schemas/sales.ts (created in Task 1 — must exist before Task 2)
    - website/src/lib/schemas/mikhmon.ts (created in Task 1)
    - docs/openapi.docs.yml (lines 4250-4736, 6173-6507 for endpoint paths/params)
    - website/src/lib/schemas/auth.ts (MessageResponseSchema import)
  </read_first>
  <action>
Create five API files with EXACT endpoint paths and EXACT schema parsing. All admin files import from `@/lib/axios/admin-client`; portal file imports from `@/lib/axios/agent-client`.

**website/src/api/sales-agent.ts:**
```typescript
import { adminClient } from '@/lib/axios/admin-client'
import {
  SalesAgentListResponseSchema,
  SalesAgentDetailResponseSchema,
  ProfilePriceListResponseSchema,
  ProfilePriceDetailResponseSchema,
  type SalesAgentResponse,
  type SalesProfilePriceResponse,
  type CreateSalesAgentRequest,
  type UpdateSalesAgentRequest,
  type UpsertProfilePriceRequest,
} from '@/lib/schemas/sales'
import { MessageResponseSchema } from '@/lib/schemas/auth'

export async function listSalesAgents(params?: { router_id?: string; limit?: number; offset?: number }): Promise<{ agents: SalesAgentResponse[]; meta?: { total: number; limit: number; offset: number } }> {
  const response = await adminClient.get('/sales-agents', { params })
  const parsed = SalesAgentListResponseSchema.parse(response.data)
  return { agents: parsed.data, meta: parsed.meta }
}

export async function getSalesAgent(id: string): Promise<SalesAgentResponse> {
  const response = await adminClient.get(`/sales-agents/${id}`)
  return SalesAgentDetailResponseSchema.parse(response.data).data
}

export async function createSalesAgent(data: CreateSalesAgentRequest): Promise<SalesAgentResponse> {
  const response = await adminClient.post('/sales-agents', data)
  return SalesAgentDetailResponseSchema.parse(response.data).data
}

export async function updateSalesAgent(id: string, data: UpdateSalesAgentRequest): Promise<SalesAgentResponse> {
  const response = await adminClient.put(`/sales-agents/${id}`, data)
  return SalesAgentDetailResponseSchema.parse(response.data).data
}

export async function deleteSalesAgent(id: string): Promise<string> {
  const response = await adminClient.delete(`/sales-agents/${id}`)
  return MessageResponseSchema.parse(response.data).data.message
}

export async function listAgentProfilePrices(agentId: string): Promise<SalesProfilePriceResponse[]> {
  const response = await adminClient.get(`/sales-agents/${agentId}/profile-prices`)
  return ProfilePriceListResponseSchema.parse(response.data).data
}

export async function upsertAgentProfilePrice(agentId: string, profileName: string, data: UpsertProfilePriceRequest): Promise<SalesProfilePriceResponse> {
  const response = await adminClient.put(`/sales-agents/${agentId}/profile-prices/${encodeURIComponent(profileName)}`, data)
  return ProfilePriceDetailResponseSchema.parse(response.data).data
}
```

**website/src/api/agent-invoice.ts:**
```typescript
import { adminClient } from '@/lib/axios/admin-client'
import {
  AgentInvoiceListResponseSchema,
  AgentInvoiceDetailResponseSchema,
  type AgentInvoiceResponse,
  type GenerateAgentInvoiceRequest,
  type MarkAgentPaidRequest,
} from '@/lib/schemas/sales'
import { MessageResponseSchema } from '@/lib/schemas/auth'

export interface AgentInvoiceFilters {
  agent_id?: string
  router_id?: string
  status?: 'draft' | 'unpaid' | 'paid' | 'cancelled'
  billing_cycle?: 'weekly' | 'monthly'
  billing_year?: number
  billing_month?: number
  billing_week?: number
  limit?: number
  offset?: number
}

export async function listAgentInvoices(filters?: AgentInvoiceFilters): Promise<{ invoices: AgentInvoiceResponse[]; meta?: { total: number; limit: number; offset: number } }> {
  const response = await adminClient.get('/agent-invoices', { params: filters })
  const parsed = AgentInvoiceListResponseSchema.parse(response.data)
  return { invoices: parsed.data, meta: parsed.meta }
}

export async function listInvoicesByAgent(agentId: string, limit?: number, offset?: number): Promise<AgentInvoiceResponse[]> {
  const params: Record<string, number> = {}
  if (limit !== undefined) params.limit = limit
  if (offset !== undefined) params.offset = offset
  const response = await adminClient.get(`/sales-agents/${agentId}/invoices`, { params })
  return AgentInvoiceListResponseSchema.parse(response.data).data
}

export async function getAgentInvoice(id: string): Promise<AgentInvoiceResponse> {
  const response = await adminClient.get(`/agent-invoices/${id}`)
  return AgentInvoiceDetailResponseSchema.parse(response.data).data
}

export async function generateAgentInvoice(agentId: string, data: GenerateAgentInvoiceRequest): Promise<AgentInvoiceResponse> {
  const response = await adminClient.post(`/sales-agents/${agentId}/invoices/generate`, data)
  return AgentInvoiceDetailResponseSchema.parse(response.data).data
}

export async function markAgentInvoicePaid(id: string, data: MarkAgentPaidRequest): Promise<AgentInvoiceResponse> {
  const response = await adminClient.put(`/agent-invoices/${id}/pay`, data)
  return AgentInvoiceDetailResponseSchema.parse(response.data).data
}

export async function cancelAgentInvoice(id: string): Promise<string> {
  const response = await adminClient.put(`/agent-invoices/${id}/cancel`)
  return MessageResponseSchema.parse(response.data).data.message
}

export async function processScheduledAgentInvoices(): Promise<string> {
  const response = await adminClient.post('/agent-invoices/process')
  return MessageResponseSchema.parse(response.data).data.message
}
```

**website/src/api/hotspot-sale.ts:**
```typescript
import { adminClient } from '@/lib/axios/admin-client'
import { HotspotSaleListResponseSchema, type HotspotSaleResponse } from '@/lib/schemas/sales'

export interface HotspotSaleFilters {
  router_id?: string
  agent_id?: string
  profile?: string
  batch_code?: string
  date_from?: string
  date_to?: string
  limit?: number
  offset?: number
}

export async function listHotspotSales(filters?: HotspotSaleFilters): Promise<{ sales: HotspotSaleResponse[]; meta?: { total: number; limit: number; offset: number } }> {
  const response = await adminClient.get('/hotspot-sales', { params: filters })
  const parsed = HotspotSaleListResponseSchema.parse(response.data)
  return { sales: parsed.data, meta: parsed.meta }
}

export async function listHotspotSalesByRouter(routerId: string, filters?: Omit<HotspotSaleFilters, 'router_id'>): Promise<HotspotSaleResponse[]> {
  const response = await adminClient.get(`/routers/${routerId}/hotspot-sales`, { params: filters })
  return HotspotSaleListResponseSchema.parse(response.data).data
}
```

**website/src/api/mikhmon.ts:**
```typescript
import { adminClient } from '@/lib/axios/admin-client'
import {
  VoucherBatchDetailResponseSchema,
  VoucherListResponseSchema,
  ScriptResponseSchema,
  ExpireStatusWrappedSchema,
  ExpireToggleResponseSchema,
  MikhmonReportSummaryWrappedSchema,
  MikhmonReportsWrappedSchema,
  type VoucherBatchResponse,
  type VoucherResponse,
  type GenerateVoucherRequest,
  type CreateMikhmonProfileRequest,
  type UpdateMikhmonProfileRequest,
  type GenerateScriptRequest,
  type ExpireStatusResponse,
  type MikhmonReportSummaryResponse,
} from '@/lib/schemas/mikhmon'
import { MessageResponseSchema } from '@/lib/schemas/auth'

// Vouchers
export async function generateVouchers(routerId: string, data: GenerateVoucherRequest): Promise<VoucherBatchResponse> {
  const response = await adminClient.post(`/routers/${routerId}/mikhmon/vouchers/generate`, data)
  return VoucherBatchDetailResponseSchema.parse(response.data).data
}

export async function getVouchers(routerId: string, params?: { comment?: string; code?: string }): Promise<VoucherResponse[]> {
  const response = await adminClient.get(`/routers/${routerId}/mikhmon/vouchers`, { params })
  return VoucherListResponseSchema.parse(response.data).data
}

export async function removeVoucherBatch(routerId: string, comment: string): Promise<string> {
  const response = await adminClient.delete(`/routers/${routerId}/mikhmon/vouchers`, { params: { comment } })
  return MessageResponseSchema.parse(response.data).data.message
}

// Profiles
export async function createMikhmonProfile(routerId: string, data: CreateMikhmonProfileRequest): Promise<string> {
  const response = await adminClient.post(`/routers/${routerId}/mikhmon/profiles`, data)
  return MessageResponseSchema.parse(response.data).data.message
}

export async function updateMikhmonProfile(routerId: string, id: string, data: UpdateMikhmonProfileRequest): Promise<string> {
  const response = await adminClient.put(`/routers/${routerId}/mikhmon/profiles/${id}`, data)
  return MessageResponseSchema.parse(response.data).data.message
}

export async function generateMikhmonScript(routerId: string, data: GenerateScriptRequest): Promise<string> {
  const response = await adminClient.post(`/routers/${routerId}/mikhmon/profiles/generate-script`, data)
  return ScriptResponseSchema.parse(response.data).data.script
}

// Reports
export async function getMikhmonReports(routerId: string, params?: { owner?: string; day?: string; user?: string; limit?: number }): Promise<unknown> {
  const response = await adminClient.get(`/routers/${routerId}/mikhmon/reports`, { params })
  return MikhmonReportsWrappedSchema.parse(response.data).data
}

export async function getMikhmonReportSummary(routerId: string): Promise<MikhmonReportSummaryResponse> {
  const response = await adminClient.get(`/routers/${routerId}/mikhmon/reports/summary`)
  return MikhmonReportSummaryWrappedSchema.parse(response.data).data
}

// Expire
export async function setupExpireMonitor(routerId: string): Promise<{ enabled: boolean }> {
  const response = await adminClient.post(`/routers/${routerId}/mikhmon/expire/setup`)
  return ExpireToggleResponseSchema.parse(response.data).data
}

export async function disableExpireMonitor(routerId: string): Promise<{ enabled: boolean }> {
  const response = await adminClient.post(`/routers/${routerId}/mikhmon/expire/disable`)
  return ExpireToggleResponseSchema.parse(response.data).data
}

export async function getExpireMonitorStatus(routerId: string): Promise<ExpireStatusResponse> {
  const response = await adminClient.get(`/routers/${routerId}/mikhmon/expire/status`)
  return ExpireStatusWrappedSchema.parse(response.data).data
}

export async function generateExpireScript(routerId: string): Promise<string> {
  const response = await adminClient.get(`/routers/${routerId}/mikhmon/expire/generate-script`)
  return ScriptResponseSchema.parse(response.data).data.script
}
```

**website/src/api/portal/agent-sales.ts:**
```typescript
import { agentClient } from '@/lib/axios/agent-client'
import { z } from 'zod'
import { hotspotSaleResponseSchema, type HotspotSaleResponse } from '@/lib/schemas/sales'

const AgentPortalSalesResponseSchema = z.object({
  success: z.boolean(),
  data: z.array(hotspotSaleResponseSchema),
})

export async function listAgentPortalSales(limit?: number, offset?: number): Promise<HotspotSaleResponse[]> {
  const params: Record<string, number> = {}
  if (limit !== undefined) params.limit = limit
  if (offset !== undefined) params.offset = offset
  const response = await agentClient.get('/sales', { params })
  return AgentPortalSalesResponseSchema.parse(response.data).data
}
```

Paths are WITHOUT the `/api/v1` prefix because axios clients already set baseURL (verify via existing `website/src/api/invoice.ts` which uses `/invoices` not `/api/v1/invoices`). Portal path is WITHOUT `/agent-portal/v1` prefix for same reason.
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/api/(sales-agent|agent-invoice|hotspot-sale|mikhmon|portal/agent-sales)\.ts" | head -5; test -z "$(cd website && npx tsc --noEmit 2>&1 | grep -E 'src/api/(sales-agent|agent-invoice|hotspot-sale|mikhmon|portal/agent-sales)\.ts')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `website/src/api/sales-agent.ts` exists and exports `listSalesAgents`, `getSalesAgent`, `createSalesAgent`, `updateSalesAgent`, `deleteSalesAgent`, `listAgentProfilePrices`, `upsertAgentProfilePrice` (grep for each `export async function`)
    - File `website/src/api/agent-invoice.ts` exports `listAgentInvoices`, `listInvoicesByAgent`, `getAgentInvoice`, `generateAgentInvoice`, `markAgentInvoicePaid`, `cancelAgentInvoice`, `processScheduledAgentInvoices`
    - File `website/src/api/hotspot-sale.ts` exports `listHotspotSales`, `listHotspotSalesByRouter`
    - File `website/src/api/mikhmon.ts` exports `generateVouchers`, `getVouchers`, `removeVoucherBatch`, `createMikhmonProfile`, `updateMikhmonProfile`, `generateMikhmonScript`, `getMikhmonReports`, `getMikhmonReportSummary`, `setupExpireMonitor`, `disableExpireMonitor`, `getExpireMonitorStatus`, `generateExpireScript`
    - File `website/src/api/portal/agent-sales.ts` exports `listAgentPortalSales`
    - All files import from `@/lib/axios/admin-client` (admin) or `@/lib/axios/agent-client` (portal only) — verify via grep `from '@/lib/axios`
    - `npx tsc --noEmit` produces no errors for these files
  </acceptance_criteria>
  <done>Five API files created, all functions exported, TS compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create TanStack Query hooks for all sales/agent/mikhmon resources</name>
  <files>website/src/hooks/use-sales-agents.ts, website/src/hooks/use-agent-invoices.ts, website/src/hooks/use-hotspot-sales.ts, website/src/hooks/use-mikhmon.ts, website/src/hooks/use-portal-sales.ts</files>
  <read_first>
    - website/src/hooks/use-invoices.ts (pattern: useQuery + useMutation with queryClient.invalidateQueries and sonner toast)
    - website/src/hooks/use-payments.ts (pattern for mutations with onSuccess invalidation arrays)
    - website/src/hooks/use-portal-billing.ts (pattern for portal hooks)
    - website/src/api/sales-agent.ts (created in Task 2)
    - website/src/api/agent-invoice.ts (created in Task 2)
    - website/src/api/hotspot-sale.ts (created in Task 2)
    - website/src/api/mikhmon.ts (created in Task 2)
    - website/src/api/portal/agent-sales.ts (created in Task 2)
  </read_first>
  <action>
Create five hook files. All mutations follow the pattern: `useMutation` + `onSuccess: queryClient.invalidateQueries` + `toast.success('...Indonesian message...')` + `onError: toast.error(narrowed message)`.

**website/src/hooks/use-sales-agents.ts:**
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
import {
  listSalesAgents,
  getSalesAgent,
  createSalesAgent,
  updateSalesAgent,
  deleteSalesAgent,
  listAgentProfilePrices,
  upsertAgentProfilePrice,
} from '@/api/sales-agent'
import type { CreateSalesAgentRequest, UpdateSalesAgentRequest, UpsertProfilePriceRequest } from '@/lib/schemas/sales'

function errMsg(err: unknown, fallback: string): string {
  return (err as { response?: { data?: { error?: string } } })?.response?.data?.error ?? fallback
}

export function useSalesAgents(params?: { router_id?: string; limit?: number; offset?: number }) {
  return useQuery({
    queryKey: ['sales-agents', params],
    queryFn: () => listSalesAgents(params),
    staleTime: 2 * 60 * 1000,
  })
}

export function useSalesAgent(id: string | null) {
  return useQuery({
    queryKey: ['sales-agents', id],
    queryFn: () => getSalesAgent(id!),
    enabled: !!id,
    staleTime: 2 * 60 * 1000,
  })
}

export function useCreateSalesAgent() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (data: CreateSalesAgentRequest) => createSalesAgent(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['sales-agents'] })
      toast.success('Agen berhasil dibuat')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membuat agen')),
  })
}

export function useUpdateSalesAgent() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateSalesAgentRequest }) => updateSalesAgent(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['sales-agents'] })
      toast.success('Agen berhasil diperbarui')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal memperbarui agen')),
  })
}

export function useDeleteSalesAgent() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (id: string) => deleteSalesAgent(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['sales-agents'] })
      toast.success('Agen berhasil dihapus')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal menghapus agen')),
  })
}

export function useAgentProfilePrices(agentId: string | null) {
  return useQuery({
    queryKey: ['sales-agents', agentId, 'profile-prices'],
    queryFn: () => listAgentProfilePrices(agentId!),
    enabled: !!agentId,
    staleTime: 2 * 60 * 1000,
  })
}

export function useUpsertAgentProfilePrice() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ agentId, profileName, data }: { agentId: string; profileName: string; data: UpsertProfilePriceRequest }) =>
      upsertAgentProfilePrice(agentId, profileName, data),
    onSuccess: (_d, vars) => {
      queryClient.invalidateQueries({ queryKey: ['sales-agents', vars.agentId, 'profile-prices'] })
      toast.success('Harga profil berhasil disimpan')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal menyimpan harga profil')),
  })
}
```

**website/src/hooks/use-agent-invoices.ts:**
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
import {
  listAgentInvoices,
  listInvoicesByAgent,
  getAgentInvoice,
  generateAgentInvoice,
  markAgentInvoicePaid,
  cancelAgentInvoice,
  processScheduledAgentInvoices,
  type AgentInvoiceFilters,
} from '@/api/agent-invoice'
import type { GenerateAgentInvoiceRequest, MarkAgentPaidRequest } from '@/lib/schemas/sales'

function errMsg(err: unknown, fallback: string): string {
  return (err as { response?: { data?: { error?: string } } })?.response?.data?.error ?? fallback
}

export function useAgentInvoices(filters?: AgentInvoiceFilters) {
  return useQuery({
    queryKey: ['agent-invoices', filters],
    queryFn: () => listAgentInvoices(filters),
    staleTime: 2 * 60 * 1000,
  })
}

export function useInvoicesByAgent(agentId: string | null, limit?: number, offset?: number) {
  return useQuery({
    queryKey: ['agent-invoices', 'by-agent', agentId, limit, offset],
    queryFn: () => listInvoicesByAgent(agentId!, limit, offset),
    enabled: !!agentId,
    staleTime: 2 * 60 * 1000,
  })
}

export function useAgentInvoice(id: string | null) {
  return useQuery({
    queryKey: ['agent-invoices', id],
    queryFn: () => getAgentInvoice(id!),
    enabled: !!id,
    staleTime: 2 * 60 * 1000,
  })
}

export function useGenerateAgentInvoice() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ agentId, data }: { agentId: string; data: GenerateAgentInvoiceRequest }) => generateAgentInvoice(agentId, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['agent-invoices'] })
      toast.success('Tagihan agen berhasil dibuat')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membuat tagihan agen')),
  })
}

export function useMarkAgentInvoicePaid() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: MarkAgentPaidRequest }) => markAgentInvoicePaid(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['agent-invoices'] })
      toast.success('Pembayaran tagihan berhasil dicatat')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal mencatat pembayaran')),
  })
}

export function useCancelAgentInvoice() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (id: string) => cancelAgentInvoice(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['agent-invoices'] })
      toast.success('Tagihan berhasil dibatalkan')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membatalkan tagihan')),
  })
}

export function useProcessScheduledAgentInvoices() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: () => processScheduledAgentInvoices(),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['agent-invoices'] })
      toast.success('Tagihan terjadwal berhasil diproses')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal memproses tagihan terjadwal')),
  })
}
```

**website/src/hooks/use-hotspot-sales.ts:**
```typescript
import { useQuery } from '@tanstack/react-query'
import { listHotspotSales, listHotspotSalesByRouter, type HotspotSaleFilters } from '@/api/hotspot-sale'

export function useHotspotSales(filters?: HotspotSaleFilters) {
  return useQuery({
    queryKey: ['hotspot-sales', filters],
    queryFn: () => listHotspotSales(filters),
    staleTime: 2 * 60 * 1000,
  })
}

export function useHotspotSalesByRouter(routerId: string | null, filters?: Omit<HotspotSaleFilters, 'router_id'>) {
  return useQuery({
    queryKey: ['hotspot-sales', 'by-router', routerId, filters],
    queryFn: () => listHotspotSalesByRouter(routerId!, filters),
    enabled: !!routerId,
    staleTime: 2 * 60 * 1000,
  })
}
```

**website/src/hooks/use-mikhmon.ts:**
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
import {
  generateVouchers,
  getVouchers,
  removeVoucherBatch,
  createMikhmonProfile,
  updateMikhmonProfile,
  generateMikhmonScript,
  getMikhmonReports,
  getMikhmonReportSummary,
  setupExpireMonitor,
  disableExpireMonitor,
  getExpireMonitorStatus,
  generateExpireScript,
} from '@/api/mikhmon'
import type { GenerateVoucherRequest, CreateMikhmonProfileRequest, UpdateMikhmonProfileRequest, GenerateScriptRequest } from '@/lib/schemas/mikhmon'

function errMsg(err: unknown, fallback: string): string {
  return (err as { response?: { data?: { error?: string } } })?.response?.data?.error ?? fallback
}

export function useMikhmonVouchers(routerId: string | null, params?: { comment?: string; code?: string }) {
  return useQuery({
    queryKey: ['mikhmon', routerId, 'vouchers', params],
    queryFn: () => getVouchers(routerId!, params),
    enabled: !!routerId,
    staleTime: 60 * 1000,
  })
}

export function useGenerateVouchers() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ routerId, data }: { routerId: string; data: GenerateVoucherRequest }) => generateVouchers(routerId, data),
    onSuccess: (_d, vars) => {
      queryClient.invalidateQueries({ queryKey: ['mikhmon', vars.routerId, 'vouchers'] })
      queryClient.invalidateQueries({ queryKey: ['hotspot-sales'] })
      toast.success('Voucher berhasil dibuat')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membuat voucher')),
  })
}

export function useRemoveVoucherBatch() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ routerId, comment }: { routerId: string; comment: string }) => removeVoucherBatch(routerId, comment),
    onSuccess: (_d, vars) => {
      queryClient.invalidateQueries({ queryKey: ['mikhmon', vars.routerId, 'vouchers'] })
      toast.success('Batch voucher berhasil dihapus')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal menghapus batch voucher')),
  })
}

export function useCreateMikhmonProfile() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ routerId, data }: { routerId: string; data: CreateMikhmonProfileRequest }) => createMikhmonProfile(routerId, data),
    onSuccess: (_d, vars) => {
      queryClient.invalidateQueries({ queryKey: ['mikhmon', vars.routerId, 'profiles'] })
      toast.success('Profil Mikhmon berhasil dibuat')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membuat profil Mikhmon')),
  })
}

export function useUpdateMikhmonProfile() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: ({ routerId, id, data }: { routerId: string; id: string; data: UpdateMikhmonProfileRequest }) => updateMikhmonProfile(routerId, id, data),
    onSuccess: (_d, vars) => {
      queryClient.invalidateQueries({ queryKey: ['mikhmon', vars.routerId, 'profiles'] })
      toast.success('Profil Mikhmon berhasil diperbarui')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal memperbarui profil Mikhmon')),
  })
}

export function useGenerateMikhmonScript() {
  return useMutation({
    mutationFn: ({ routerId, data }: { routerId: string; data: GenerateScriptRequest }) => generateMikhmonScript(routerId, data),
    onSuccess: () => toast.success('Skrip berhasil dibuat'),
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membuat skrip')),
  })
}

export function useMikhmonReports(routerId: string | null, params?: { owner?: string; day?: string; user?: string; limit?: number }) {
  return useQuery({
    queryKey: ['mikhmon', routerId, 'reports', params],
    queryFn: () => getMikhmonReports(routerId!, params),
    enabled: !!routerId,
    staleTime: 60 * 1000,
  })
}

export function useMikhmonReportSummary(routerId: string | null) {
  return useQuery({
    queryKey: ['mikhmon', routerId, 'reports', 'summary'],
    queryFn: () => getMikhmonReportSummary(routerId!),
    enabled: !!routerId,
    staleTime: 60 * 1000,
  })
}

export function useExpireMonitorStatus(routerId: string | null) {
  return useQuery({
    queryKey: ['mikhmon', routerId, 'expire', 'status'],
    queryFn: () => getExpireMonitorStatus(routerId!),
    enabled: !!routerId,
    staleTime: 30 * 1000,
  })
}

export function useSetupExpireMonitor() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (routerId: string) => setupExpireMonitor(routerId),
    onSuccess: (_d, routerId) => {
      queryClient.invalidateQueries({ queryKey: ['mikhmon', routerId, 'expire', 'status'] })
      toast.success('Monitor kedaluwarsa berhasil diaktifkan')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal mengaktifkan monitor kedaluwarsa')),
  })
}

export function useDisableExpireMonitor() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (routerId: string) => disableExpireMonitor(routerId),
    onSuccess: (_d, routerId) => {
      queryClient.invalidateQueries({ queryKey: ['mikhmon', routerId, 'expire', 'status'] })
      toast.success('Monitor kedaluwarsa berhasil dinonaktifkan')
    },
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal menonaktifkan monitor kedaluwarsa')),
  })
}

export function useGenerateExpireScript() {
  return useMutation({
    mutationFn: (routerId: string) => generateExpireScript(routerId),
    onSuccess: () => toast.success('Skrip kedaluwarsa berhasil dibuat'),
    onError: (err: unknown) => toast.error(errMsg(err, 'Gagal membuat skrip kedaluwarsa')),
  })
}
```

**website/src/hooks/use-portal-sales.ts:**
```typescript
import { useQuery } from '@tanstack/react-query'
import { listAgentPortalSales } from '@/api/portal/agent-sales'

export function useAgentPortalSales(limit?: number, offset?: number) {
  return useQuery({
    queryKey: ['agent-portal-sales', limit, offset],
    queryFn: () => listAgentPortalSales(limit, offset),
    staleTime: 2 * 60 * 1000,
  })
}
```
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/hooks/use-(sales-agents|agent-invoices|hotspot-sales|mikhmon|portal-sales)\.ts" | head -5; test -z "$(cd website && npx tsc --noEmit 2>&1 | grep -E 'src/hooks/use-(sales-agents|agent-invoices|hotspot-sales|mikhmon|portal-sales)\.ts')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `website/src/hooks/use-sales-agents.ts` exports `useSalesAgents`, `useSalesAgent`, `useCreateSalesAgent`, `useUpdateSalesAgent`, `useDeleteSalesAgent`, `useAgentProfilePrices`, `useUpsertAgentProfilePrice`
    - File `website/src/hooks/use-agent-invoices.ts` exports `useAgentInvoices`, `useInvoicesByAgent`, `useAgentInvoice`, `useGenerateAgentInvoice`, `useMarkAgentInvoicePaid`, `useCancelAgentInvoice`, `useProcessScheduledAgentInvoices`
    - File `website/src/hooks/use-hotspot-sales.ts` exports `useHotspotSales`, `useHotspotSalesByRouter`
    - File `website/src/hooks/use-mikhmon.ts` exports at least: `useMikhmonVouchers`, `useGenerateVouchers`, `useRemoveVoucherBatch`, `useCreateMikhmonProfile`, `useUpdateMikhmonProfile`, `useGenerateMikhmonScript`, `useMikhmonReports`, `useMikhmonReportSummary`, `useExpireMonitorStatus`, `useSetupExpireMonitor`, `useDisableExpireMonitor`, `useGenerateExpireScript`
    - File `website/src/hooks/use-portal-sales.ts` exports `useAgentPortalSales`
    - All mutations use `toast.success` with Indonesian text (grep for `toast.success\('[A-Za-z]` returns matches with "berhasil")
    - `npx tsc --noEmit` passes cleanly
  </acceptance_criteria>
  <done>Five hook files created, mutation invalidation and Indonesian toasts in place, no TS errors.</done>
</task>

</tasks>

<verification>
Run `cd website && npx tsc --noEmit` and confirm zero errors in all 12 new files. Grep for each expected export to confirm presence.
</verification>

<success_criteria>
All Phase 5 Zod schemas, API functions, and TanStack Query hooks exist. Downstream plans (05-02..05-06) can import hooks without needing further data-layer work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-sales-agents/05-01-SUMMARY.md`
</output>
