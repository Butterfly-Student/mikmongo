---
phase: 05-sales-agents
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - website/src/features/sales/agents/index.tsx
  - website/src/features/sales/agents/data/schema.ts
  - website/src/features/sales/agents/data/columns.tsx
  - website/src/features/sales/agents/components/agents-table.tsx
  - website/src/features/sales/agents/components/create-agent-dialog.tsx
  - website/src/features/sales/agents/components/edit-agent-dialog.tsx
  - website/src/features/sales/agents/components/profile-prices-sheet.tsx
  - website/src/routes/_authenticated/agents/index.tsx
  - website/src/components/layout/data/sidebar-data.ts
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03]
must_haves:
  truths:
    - "Admin can view a paginated, filterable list of sales agents at /agents"
    - "Admin can create a sales agent via a dialog with router selection and full config (voucher mode/length/type, billing cycle/day, bill discount)"
    - "Admin can edit an existing sales agent"
    - "Admin can view and upsert profile prices (base, selling, voucher_length, is_active) per bandwidth profile for an agent via a side sheet"
  artifacts:
    - path: "website/src/features/sales/agents/index.tsx"
      provides: "Agents list page (header + table)"
    - path: "website/src/features/sales/agents/components/create-agent-dialog.tsx"
      provides: "Create agent dialog with RHF + Zod"
    - path: "website/src/features/sales/agents/components/edit-agent-dialog.tsx"
      provides: "Edit agent dialog"
    - path: "website/src/features/sales/agents/components/profile-prices-sheet.tsx"
      provides: "Profile-prices management side sheet with upsert rows"
    - path: "website/src/routes/_authenticated/agents/index.tsx"
      provides: "Route entry /agents"
  key_links:
    - from: "website/src/features/sales/agents/index.tsx"
      to: "useSalesAgents"
      via: "TanStack Query hook"
      pattern: "useSalesAgents\\("
    - from: "website/src/features/sales/agents/components/profile-prices-sheet.tsx"
      to: "useAgentProfilePrices, useUpsertAgentProfilePrice, useProfiles"
      via: "TanStack Query"
      pattern: "useUpsertAgentProfilePrice\\("
    - from: "website/src/components/layout/data/sidebar-data.ts"
      to: "/agents"
      via: "sidebar nav item"
      pattern: "url:\\s*'/agents'"
---

<objective>
Build the admin Sales Agent management page: list with TanStack Table, create/edit dialogs, and per-agent profile-price management via side sheet. Enable the previously-disabled "Agents" sidebar item.

Purpose: Admin needs to create/configure sales agents and set per-profile pricing before generating agent invoices.
Output: /agents page fully functional; Agents sidebar link enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-sales-agents/05-CONTEXT.md
@website/src/features/customers/index.tsx
@website/src/features/routers/index.tsx
@website/src/features/billing/invoices/index.tsx
@website/src/features/billing/invoices/components/invoice-table.tsx
@website/src/features/billing/invoices/components/invoice-detail-sheet.tsx

<interfaces>
From Task 1-3 of 05-01 (data layer):

```typescript
// Hooks available
import { useSalesAgents, useCreateSalesAgent, useUpdateSalesAgent, useAgentProfilePrices, useUpsertAgentProfilePrice } from '@/hooks/use-sales-agents'
import { useRouters } from '@/hooks/use-routers'
import { useProfiles } from '@/hooks/use-profiles' // existing from Phase 3 — bandwidth profiles per router

// Types
import type { SalesAgentResponse, CreateSalesAgentRequest, UpdateSalesAgentRequest, SalesProfilePriceResponse, UpsertProfilePriceRequest } from '@/lib/schemas/sales'

// Request shape
interface CreateSalesAgentRequest {
  router_id: string         // required
  name: string              // required
  phone?: string
  username: string          // required
  password: string          // required, min 6
  status?: 'active' | 'inactive'
  voucher_mode?: 'mix' | 'num' | 'alp'
  voucher_length?: number
  voucher_type?: 'upp' | 'up'
  bill_discount?: number
  billing_cycle?: 'weekly' | 'monthly'
  billing_day?: number      // 1-31
}
```

Follow patterns from `website/src/features/customers/index.tsx` (list + create + edit dialog + TanStack Table with faceted filter).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build agents list page, table, columns, and route</name>
  <files>website/src/features/sales/agents/index.tsx, website/src/features/sales/agents/data/columns.tsx, website/src/features/sales/agents/components/agents-table.tsx, website/src/routes/_authenticated/agents/index.tsx, website/src/components/layout/data/sidebar-data.ts</files>
  <read_first>
    - website/src/features/billing/invoices/index.tsx (page structure: Header + Main + toolbar + table + sheet)
    - website/src/features/billing/invoices/components/invoice-table.tsx (TanStack Table with DataTableToolbar + faceted filter)
    - website/src/features/billing/invoices/data/columns.tsx (column definitions with badges, ids, formatters)
    - website/src/features/customers/index.tsx (CRUD page structure with dialogs)
    - website/src/routes/_authenticated/invoices/index.tsx (route file pattern)
    - website/src/components/layout/data/sidebar-data.ts (MUST update to enable /agents)
  </read_first>
  <action>
Create feature structure and route.

**website/src/features/sales/agents/index.tsx:**
- Default-exported React component `SalesAgentsPage`
- Uses `useSalesAgents()` hook
- Header with title "Manajemen Agen" (subtitle "Kelola agen penjualan dan harga profil mereka") and a "Tambah Agen" button that opens `CreateAgentDialog`
- State: `editTarget: SalesAgentResponse | null`, `pricesTarget: SalesAgentResponse | null`, `createOpen: boolean`
- Skeleton when `isLoading`; pass agents to `AgentsTable` with callbacks `onEdit(agent)` → setEditTarget, `onManagePrices(agent)` → setPricesTarget
- Renders `<CreateAgentDialog open={createOpen} onOpenChange={setCreateOpen} />`, `<EditAgentDialog agent={editTarget} onClose={() => setEditTarget(null)} />`, `<ProfilePricesSheet agent={pricesTarget} onClose={() => setPricesTarget(null)} />`

**website/src/features/sales/agents/data/columns.tsx:**
Define `createAgentColumns(opts: { onEdit: (a: SalesAgentResponse) => void; onManagePrices: (a: SalesAgentResponse) => void }): ColumnDef<SalesAgentResponse>[]` with columns:
- `name` (header "Nama", accessorKey: 'name')
- `username` (header "Username")
- `phone` (header "Telepon", cell returns value or "-")
- `status` with enableColumnFilter:true, filterFn:'equals', header "Status", cell renders Badge: active → green "Aktif", inactive → gray "Nonaktif"
- `billing_cycle` with header "Siklus", cell: weekly→"Mingguan", monthly→"Bulanan", null→"-"
- `billing_day` with header "Hari Tagihan"
- `bill_discount` with header "Diskon", cell: `${(v * 100).toFixed(0)}%` if v<1 else `Rp ${v.toLocaleString('id-ID')}`, null→"-"
- `actions` with header empty, cell renders DropdownMenu with items: "Edit" (calls opts.onEdit), "Kelola Harga Profil" (calls opts.onManagePrices)

**website/src/features/sales/agents/components/agents-table.tsx:**
- Props: `agents: SalesAgentResponse[]`, `onEdit: (a: SalesAgentResponse) => void`, `onManagePrices: (a: SalesAgentResponse) => void`
- Use `useReactTable` with `getCoreRowModel`, `getPaginationRowModel`, `getFilteredRowModel`, `getFacetedRowModel`, `getFacetedUniqueValues`
- DataTableToolbar with FacetedFilter for `status` column (options: [{label:'Aktif',value:'active'},{label:'Nonaktif',value:'inactive'}]) and search input on `name` column
- Render `<Table>` with standard pagination controls (reuse from existing billing tables)

**website/src/routes/_authenticated/agents/index.tsx:**
```typescript
import { createFileRoute } from '@tanstack/react-router'
import SalesAgentsPage from '@/features/sales/agents'
export const Route = createFileRoute('/_authenticated/agents/')({
  component: SalesAgentsPage,
})
```

**website/src/components/layout/data/sidebar-data.ts:**
In the 'Sales' nav group, change the "Agents" item FROM:
```
{ title: 'Agents', url: '#', icon: UserCog, disabled: true }
```
TO:
```
{ title: 'Agen', url: '/agents', icon: UserCog }
```
Remove the `disabled: true` field and set `url: '/agents'`. Change title text to Indonesian "Agen".
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/features/sales/agents|src/routes/_authenticated/agents" | head -10; grep -n "url: '/agents'" website/src/components/layout/data/sidebar-data.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `website/src/features/sales/agents/index.tsx` exists with default export
    - File `website/src/features/sales/agents/data/columns.tsx` exports `createAgentColumns`
    - File `website/src/features/sales/agents/components/agents-table.tsx` exports `AgentsTable` component
    - File `website/src/routes/_authenticated/agents/index.tsx` exists and references `SalesAgentsPage`
    - grep `"Manajemen Agen"` website/src/features/sales/agents/index.tsx returns match
    - grep `url: '/agents'` website/src/components/layout/data/sidebar-data.ts returns match
    - grep `disabled: true` website/src/components/layout/data/sidebar-data.ts does NOT match on any line that also matches "Agents" or "Agen" (the Agen item must not have `disabled: true`)
    - No `disabled` key on the Agen sidebar item (verify with `grep -A2 "'Agen'" website/src/components/layout/data/sidebar-data.ts | grep disabled` returns empty)
    - `npx tsc --noEmit` passes for these files
  </acceptance_criteria>
  <done>/agents page renders list with filters; sidebar link enabled.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build create/edit agent dialogs and profile-prices sheet</name>
  <files>website/src/features/sales/agents/components/create-agent-dialog.tsx, website/src/features/sales/agents/components/edit-agent-dialog.tsx, website/src/features/sales/agents/components/profile-prices-sheet.tsx</files>
  <read_first>
    - website/src/features/sales/agents/index.tsx (created in Task 1 — dialog wiring)
    - website/src/features/customers/components/create-customer-dialog.tsx (RHF + zodResolver + Dialog pattern)
    - website/src/features/customers/components/edit-customer-dialog.tsx (useEffect reset on prop change, password field optional)
    - website/src/hooks/use-sales-agents.ts (hook signatures)
    - website/src/hooks/use-routers.ts (for router select)
    - website/src/hooks/use-profiles.ts (for per-router profile list in prices sheet)
    - website/src/components/ui/sheet.tsx (Sheet API)
    - website/src/lib/schemas/sales.ts (createSalesAgentSchema, updateSalesAgentSchema, upsertProfilePriceSchema for form validation)
  </read_first>
  <action>
**website/src/features/sales/agents/components/create-agent-dialog.tsx:**
- Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`
- React Hook Form + zodResolver using `createSalesAgentSchema`
- Fields: `router_id` (Select from `useRouters()`), `name` (Input), `username` (Input), `password` (Input type=password), `phone` (Input), `status` (Select: active/inactive, default 'active'), `voucher_mode` (Select: mix/num/alp), `voucher_length` (Input type=number), `voucher_type` (Select: upp/up), `bill_discount` (Input type=number step=0.01), `billing_cycle` (Select: weekly/monthly), `billing_day` (Input type=number min=1 max=31)
- On submit: `useCreateSalesAgent().mutateAsync(data)`, on success reset form and call `onOpenChange(false)`
- Dialog title: "Tambah Agen Baru"; submit button: "Simpan" (loading: "Menyimpan...")
- Remove undefined/empty optional fields from payload before mutation (backend treats missing differently from null)

**website/src/features/sales/agents/components/edit-agent-dialog.tsx:**
- Props: `{ agent: SalesAgentResponse | null; onClose: () => void }` (agent doubles as open flag)
- RHF + zodResolver using `updateSalesAgentSchema`
- `useEffect` resets form when `agent` changes with values: name, phone, status, voucher_mode, voucher_length, voucher_type, bill_discount, billing_cycle, billing_day, password: ''
- `router_id` and `username` shown as read-only (cannot change per API contract)
- Password field empty by default; if user fills it, include in payload; otherwise omit (mirrors customer edit pattern)
- On submit: `useUpdateSalesAgent().mutateAsync({ id: agent.id, data: cleanedPayload })`, on success call `onClose()`
- Dialog title: "Edit Agen"; submit button: "Simpan Perubahan"

**website/src/features/sales/agents/components/profile-prices-sheet.tsx:**
- Props: `{ agent: SalesAgentResponse | null; onClose: () => void }`
- Side Sheet (right side, w-[480px] sm:max-w-lg)
- Title: `Harga Profil — ${agent.name}`, description: "Atur harga dasar dan jual untuk setiap profil bandwidth"
- Fetches two datasets:
  - `useAgentProfilePrices(agent?.id ?? null)` → existing price rows (`SalesProfilePriceResponse[]`)
  - `useProfiles(agent?.router_id ?? null)` → bandwidth profiles available on the agent's router (existing Phase 3 hook)
- Build a merged list: for each bandwidth profile on the router, show a row with:
  - profile_name (label)
  - `base_price` input (type=number, Rp prefix)
  - `selling_price` input (type=number, Rp prefix)
  - `voucher_length` input (type=number, optional)
  - `is_active` Switch (default true)
  - "Simpan" button per row that calls `useUpsertAgentProfilePrice().mutate({ agentId: agent.id, profileName: profile.name, data: {...} })`
- If an existing price row matches `profile_name`, prefill inputs from it; otherwise start blank (defaults: is_active=true)
- Each row has local React state for its own inputs
- Skeleton while either query loading
- Show message "Agen belum memiliki router terpilih" if `agent.router_id` falsy
- Close via Sheet's onOpenChange → call onClose
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/features/sales/agents/components/(create-agent-dialog|edit-agent-dialog|profile-prices-sheet)" | head -10; test -z "$(cd website && npx tsc --noEmit 2>&1 | grep -E 'src/features/sales/agents/components/(create-agent-dialog|edit-agent-dialog|profile-prices-sheet)')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `create-agent-dialog.tsx` renders a Dialog with form fields for all 12 CreateSalesAgentRequest fields (grep for `name='router_id'`, `name='name'`, `name='username'`, `name='password'`, `name='voucher_mode'`, `name='billing_cycle'`, `name='billing_day'`, `name='bill_discount'`, `name='voucher_length'`, `name='voucher_type'`, `name='phone'`, `name='status'`)
    - `edit-agent-dialog.tsx` uses `useUpdateSalesAgent` hook (grep returns match)
    - `edit-agent-dialog.tsx` uses `useEffect` to reset form on agent prop change (grep `useEffect` and `form.reset` both return matches)
    - `profile-prices-sheet.tsx` uses both `useAgentProfilePrices` and `useProfiles` and `useUpsertAgentProfilePrice` (grep returns all three)
    - `profile-prices-sheet.tsx` contains Indonesian title substring "Harga Profil" (grep match)
    - All dialog titles and buttons contain Indonesian text (grep "Tambah Agen Baru|Edit Agen|Simpan" returns matches)
    - `npx tsc --noEmit` passes clean for these files
  </acceptance_criteria>
  <done>Create, edit, and profile-prices UI functional against data layer.</done>
</task>

</tasks>

<verification>
Navigate to /agents, verify list renders, open create dialog, edit an agent, and manage profile prices for an agent whose router has bandwidth profiles. Sidebar shows "Agen" link enabled.
</verification>

<success_criteria>
Admin can list/filter sales agents, create new agents with full config, edit existing agents, and upsert per-profile prices per agent.
</success_criteria>

<output>
After completion, create `.planning/phases/05-sales-agents/05-02-SUMMARY.md`
</output>
