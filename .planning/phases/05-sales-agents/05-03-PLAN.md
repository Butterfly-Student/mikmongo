---
phase: 05-sales-agents
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - website/src/features/sales/agent-invoices/index.tsx
  - website/src/features/sales/agent-invoices/data/columns.tsx
  - website/src/features/sales/agent-invoices/components/agent-invoices-table.tsx
  - website/src/features/sales/agent-invoices/components/agent-invoice-detail-sheet.tsx
  - website/src/features/sales/agent-invoices/components/generate-agent-invoice-dialog.tsx
  - website/src/features/sales/agent-invoices/components/mark-paid-dialog.tsx
  - website/src/features/sales/agent-invoices/components/agent-invoice-action-menu.tsx
  - website/src/features/sales/agent-invoices/components/process-scheduled-button.tsx
  - website/src/routes/_authenticated/agent-invoices/index.tsx
  - website/src/components/layout/data/sidebar-data.ts
autonomous: true
requirements: [AGNT-04, AGNT-05, AGNT-06]
must_haves:
  truths:
    - "Admin can view agent invoices with filters (agent, router, status, billing cycle, year/month/week) at /agent-invoices"
    - "Admin can open an invoice detail side sheet with full breakdown (voucher_count, subtotal, selling_total, profit, discount, total, paid, balance)"
    - "Admin can generate a new agent invoice for a given agent over a date range via dialog"
    - "Admin can mark an agent invoice as paid (with paid_amount)"
    - "Admin can cancel an agent invoice with confirmation"
    - "Admin can trigger 'Proses Tagihan Terjadwal' to process scheduled agent invoices"
  artifacts:
    - path: "website/src/features/sales/agent-invoices/index.tsx"
      provides: "Agent invoices list page"
    - path: "website/src/features/sales/agent-invoices/components/generate-agent-invoice-dialog.tsx"
      provides: "Generate invoice dialog with agent+date-range selection"
    - path: "website/src/features/sales/agent-invoices/components/mark-paid-dialog.tsx"
      provides: "Mark paid dialog with amount input"
    - path: "website/src/features/sales/agent-invoices/components/agent-invoice-detail-sheet.tsx"
      provides: "Detail side sheet with financial breakdown"
    - path: "website/src/routes/_authenticated/agent-invoices/index.tsx"
      provides: "Route entry /agent-invoices"
  key_links:
    - from: "website/src/features/sales/agent-invoices/index.tsx"
      to: "useAgentInvoices"
      via: "TanStack Query"
      pattern: "useAgentInvoices\\("
    - from: "website/src/features/sales/agent-invoices/components/generate-agent-invoice-dialog.tsx"
      to: "useGenerateAgentInvoice, useSalesAgents"
      via: "hooks"
      pattern: "useGenerateAgentInvoice\\("
    - from: "website/src/components/layout/data/sidebar-data.ts"
      to: "/agent-invoices"
      via: "sidebar nav (new item in Sales group)"
      pattern: "url:\\s*'/agent-invoices'"
---

<objective>
Build agent invoice management: list with faceted filters, detail sheet, generate dialog, payment workflow (pay/cancel/process). Adds a new "Tagihan Agen" item to the Sales sidebar group.

Purpose: Admin must generate and manage invoices for sales agents based on their voucher sales.
Output: /agent-invoices page with full CRUD + workflow actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-sales-agents/05-CONTEXT.md
@website/src/features/billing/invoices/index.tsx
@website/src/features/billing/invoices/components/invoice-detail-sheet.tsx
@website/src/features/billing/payments/components/payment-action-menu.tsx
@website/src/features/billing/payments/components/confirm-payment-dialog.tsx

<interfaces>
From 05-01 data layer:

```typescript
import { useAgentInvoices, useAgentInvoice, useGenerateAgentInvoice, useMarkAgentInvoicePaid, useCancelAgentInvoice, useProcessScheduledAgentInvoices } from '@/hooks/use-agent-invoices'
import { useSalesAgents } from '@/hooks/use-sales-agents'
import type { AgentInvoiceResponse, GenerateAgentInvoiceRequest, MarkAgentPaidRequest } from '@/lib/schemas/sales'
import type { AgentInvoiceFilters } from '@/api/agent-invoice'

// Status enum: 'draft' | 'unpaid' | 'paid' | 'cancelled'
// Billing cycle: 'weekly' | 'monthly'
// AgentInvoiceResponse fields: invoice_number, agent_id, router_id, period_start, period_end, voucher_count, subtotal, selling_total, profit, discount_amount, total_amount, paid_amount, balance, status, notes
```

Status labels (Indonesian):
- draft → "Draft" gray
- unpaid → "Belum Lunas" yellow
- paid → "Lunas" green
- cancelled → "Dibatalkan" red
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build agent invoices list, columns, table, detail sheet, and route</name>
  <files>website/src/features/sales/agent-invoices/index.tsx, website/src/features/sales/agent-invoices/data/columns.tsx, website/src/features/sales/agent-invoices/components/agent-invoices-table.tsx, website/src/features/sales/agent-invoices/components/agent-invoice-detail-sheet.tsx, website/src/routes/_authenticated/agent-invoices/index.tsx, website/src/components/layout/data/sidebar-data.ts</files>
  <read_first>
    - website/src/features/billing/invoices/index.tsx (page + state for selected + sheet)
    - website/src/features/billing/invoices/components/invoice-table.tsx (faceted filter + table pagination)
    - website/src/features/billing/invoices/components/invoice-detail-sheet.tsx (grid breakdown pattern)
    - website/src/features/billing/invoices/data/columns.tsx (badges + formatRp helper)
    - website/src/components/layout/data/sidebar-data.ts (add new sidebar item after Agen)
  </read_first>
  <action>
**website/src/features/sales/agent-invoices/index.tsx:**
- Default export `AgentInvoicesPage`
- State: `selected: AgentInvoiceResponse | null`, `generateOpen: boolean`, `markPaidTarget: AgentInvoiceResponse | null`, `cancelTarget: AgentInvoiceResponse | null`
- Header "Tagihan Agen" (subtitle "Kelola tagihan untuk agen penjualan"); header-right buttons: `<ProcessScheduledButton />` + "Buat Tagihan" (opens generate dialog)
- Use `useAgentInvoices()` without filters (table does client filtering via faceted filters)
- Render `AgentInvoicesTable` with agents data, `onRowClick`, `onMarkPaid`, `onCancel`
- Render `AgentInvoiceDetailSheet` (open when `selected`), `GenerateAgentInvoiceDialog`, `MarkPaidDialog` (for markPaidTarget), AlertDialog for cancel confirmation calling `useCancelAgentInvoice().mutate(cancelTarget.id)`

**website/src/features/sales/agent-invoices/data/columns.tsx:**
Export `createAgentInvoiceColumns(opts: { onMarkPaid: (inv) => void; onCancel: (inv) => void }): ColumnDef<AgentInvoiceResponse>[]` with:
- `invoice_number` (header "No. Tagihan")
- `agent_id` (header "Agen", displays shortened id; later plan could enhance — for now show last 8 chars of uuid with prefix "...")
- `billing_cycle` (header "Siklus", weekly→"Mingguan", monthly→"Bulanan", enableColumnFilter, filterFn 'equals')
- `period_start` / `period_end` combined: header "Periode", cell: `${format(parseISO(period_start),'dd/MM/yyyy')} - ${format(parseISO(period_end),'dd/MM/yyyy')}`
- `voucher_count` (header "Voucher")
- `total_amount` (header "Total", formatRp)
- `paid_amount` (header "Dibayar", formatRp)
- `balance` (header "Saldo", formatRp)
- `status` with filterFn 'equals' and Badge: draft/unpaid/paid/cancelled mapping to Indonesian labels (see interfaces)
- `actions` with DropdownMenu calling opts.onMarkPaid (only when status=='unpaid'|'draft') and opts.onCancel (only when status!='paid' && status!='cancelled')

**website/src/features/sales/agent-invoices/components/agent-invoices-table.tsx:**
- Props `{ invoices, onRowClick, onMarkPaid, onCancel }`
- TanStack Table with `getCoreRowModel`, pagination, filtered, faceted
- DataTableToolbar with FacetedFilter on `status` (4 options), FacetedFilter on `billing_cycle` (2 options), search input on `invoice_number`
- Row click calls `onRowClick(row.original)`

**website/src/features/sales/agent-invoices/components/agent-invoice-detail-sheet.tsx:**
- Props `{ invoice: AgentInvoiceResponse | null; onClose: () => void }`
- Side sheet (right) showing:
  - Title: `Tagihan ${invoice.invoice_number}`
  - Status badge
  - Period range
  - Grid 2-col with labels (Indonesian): Voucher, Subtotal, Total Jual, Profit, Diskon, Total, Dibayar, Saldo — values formatted with formatRp (or plain for voucher_count)
  - If notes present, show in a section "Catatan"

**website/src/routes/_authenticated/agent-invoices/index.tsx:**
```typescript
import { createFileRoute } from '@tanstack/react-router'
import AgentInvoicesPage from '@/features/sales/agent-invoices'
export const Route = createFileRoute('/_authenticated/agent-invoices/')({ component: AgentInvoicesPage })
```

**website/src/components/layout/data/sidebar-data.ts:**
In the 'Sales' nav group, insert a NEW item AFTER the Agen item and BEFORE "Hotspot Sales":
```
{ title: 'Tagihan Agen', url: '/agent-invoices', icon: FileText }
```
Ensure `FileText` is already imported (it is, for billing Invoices). Do NOT set `disabled: true` on this new item.
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/features/sales/agent-invoices|src/routes/_authenticated/agent-invoices" | head -10; grep -n "url: '/agent-invoices'" website/src/components/layout/data/sidebar-data.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `website/src/features/sales/agent-invoices/index.tsx` exists with default export
    - File `website/src/features/sales/agent-invoices/data/columns.tsx` exports `createAgentInvoiceColumns`
    - File `website/src/features/sales/agent-invoices/components/agent-invoices-table.tsx` exists
    - File `website/src/features/sales/agent-invoices/components/agent-invoice-detail-sheet.tsx` exists
    - File `website/src/routes/_authenticated/agent-invoices/index.tsx` exists
    - grep "'Tagihan Agen'" website/src/components/layout/data/sidebar-data.ts returns match
    - grep "url: '/agent-invoices'" website/src/components/layout/data/sidebar-data.ts returns match
    - Detail sheet contains Indonesian labels: grep "Voucher\|Subtotal\|Total Jual\|Profit\|Diskon\|Dibayar\|Saldo" in agent-invoice-detail-sheet.tsx returns 7+ matches
    - Status labels: grep "Belum Lunas\|Lunas\|Dibatalkan" in columns.tsx returns 3 matches
    - `npx tsc --noEmit` clean for these files
  </acceptance_criteria>
  <done>Agent invoices list + detail + sidebar link functional.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build generate, mark-paid, action menu, process-scheduled components</name>
  <files>website/src/features/sales/agent-invoices/components/generate-agent-invoice-dialog.tsx, website/src/features/sales/agent-invoices/components/mark-paid-dialog.tsx, website/src/features/sales/agent-invoices/components/agent-invoice-action-menu.tsx, website/src/features/sales/agent-invoices/components/process-scheduled-button.tsx</files>
  <read_first>
    - website/src/features/sales/agent-invoices/index.tsx (created in Task 1 — wiring)
    - website/src/features/billing/invoices/components/invoice-generation-trigger.tsx (trigger button pattern)
    - website/src/features/billing/payments/components/confirm-payment-dialog.tsx (dialog pattern with form + mutation)
    - website/src/features/billing/payments/components/payment-action-menu.tsx (DropdownMenu pattern)
    - website/src/hooks/use-agent-invoices.ts (all hooks)
    - website/src/hooks/use-sales-agents.ts (for agent select)
    - website/src/lib/schemas/sales.ts (generateAgentInvoiceSchema, markAgentPaidSchema)
  </read_first>
  <action>
**website/src/features/sales/agent-invoices/components/generate-agent-invoice-dialog.tsx:**
- Props: `{ open: boolean; onOpenChange: (o: boolean) => void }`
- Form (RHF + zodResolver(generateAgentInvoiceSchema)) with fields + additional agent selector:
  - `agent_id` (Select, options from `useSalesAgents()`, label "Agen")
  - `period_start` (Input type=date, label "Mulai")
  - `period_end` (Input type=date, label "Selesai")
- On submit: `useGenerateAgentInvoice().mutateAsync({ agentId: values.agent_id, data: { period_start, period_end } })`, on success reset+close
- Dialog title "Buat Tagihan Agen"; submit button "Buat Tagihan" (loading "Membuat...")
- Validate agent_id is required (separate Zod: `z.object({ agent_id: z.string().uuid(), period_start: z.string(), period_end: z.string() })`)

**website/src/features/sales/agent-invoices/components/mark-paid-dialog.tsx:**
- Props: `{ invoice: AgentInvoiceResponse | null; onClose: () => void }` (invoice is both the target and open flag)
- Form RHF + zodResolver(markAgentPaidSchema) with `paid_amount` input (type=number, step=0.01, min=0)
- Default value: `invoice?.balance ?? 0` (prefill remaining balance)
- useEffect resets form when invoice prop changes
- On submit: `useMarkAgentInvoicePaid().mutateAsync({ id: invoice.id, data: { paid_amount } })`, on success onClose()
- Dialog title: `Catat Pembayaran — ${invoice?.invoice_number}`
- Show invoice context (Total, Saldo) above the input

**website/src/features/sales/agent-invoices/components/agent-invoice-action-menu.tsx:**
- Props: `{ invoice: AgentInvoiceResponse; onMarkPaid: (i) => void; onCancel: (i) => void }`
- DropdownMenu with MoreHorizontal trigger
- "Catat Pembayaran" item (enabled only if status in ['draft','unpaid']) → onMarkPaid(invoice)
- "Batalkan Tagihan" item (enabled only if status not in ['paid','cancelled']) → onCancel(invoice), className='text-destructive'

**website/src/features/sales/agent-invoices/components/process-scheduled-button.tsx:**
- Button component: `<Button variant='outline' onClick={() => mutate()}>Proses Tagihan Terjadwal</Button>`
- Uses `useProcessScheduledAgentInvoices()` hook
- Shows loading state ("Memproses...") while mutation pending
- No props
  </action>
  <verify>
    <automated>cd website && npx tsc --noEmit 2>&1 | grep -E "src/features/sales/agent-invoices/components" | head -10; test -z "$(cd website && npx tsc --noEmit 2>&1 | grep -E 'src/features/sales/agent-invoices/components')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `generate-agent-invoice-dialog.tsx` uses `useGenerateAgentInvoice` AND `useSalesAgents` (both greps return match)
    - `generate-agent-invoice-dialog.tsx` has input with `type='date'` and `name='period_start'` and `name='period_end'` (greps return matches)
    - `mark-paid-dialog.tsx` uses `useMarkAgentInvoicePaid` and has `paid_amount` input
    - `mark-paid-dialog.tsx` contains `useEffect` and `form.reset` (grep matches)
    - `agent-invoice-action-menu.tsx` uses DropdownMenu and contains string "Catat Pembayaran" and "Batalkan Tagihan"
    - `process-scheduled-button.tsx` uses `useProcessScheduledAgentInvoices` and contains text "Proses Tagihan Terjadwal"
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Generate, mark paid, cancel, and process scheduled all wired to mutations.</done>
</task>

</tasks>

<verification>
Navigate to /agent-invoices, verify list + filters, open detail sheet, generate a new invoice for a seeded agent, mark one as paid, cancel another, and click "Proses Tagihan Terjadwal". All actions should show Indonesian success toast and list updates.
</verification>

<success_criteria>
Admin can view agent invoices with filters, see detail breakdown, generate new invoices for period, mark as paid, cancel, and process scheduled.
</success_criteria>

<output>
After completion, create `.planning/phases/05-sales-agents/05-03-SUMMARY.md`
</output>
