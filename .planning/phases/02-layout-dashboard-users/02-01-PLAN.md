---
phase: 02-layout-dashboard-users
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/stores/router-store.ts
  - website/src/lib/schemas/router.ts
  - website/src/lib/schemas/user.ts
  - website/src/lib/schemas/report.ts
  - website/src/api/router.ts
  - website/src/api/report.ts
  - website/src/api/user.ts
  - website/src/api/types.ts
  - website/src/hooks/use-routers.ts
  - website/src/hooks/use-report-summary.ts
  - website/src/hooks/use-users.ts
autonomous: true
requirements: [NAV-01, NAV-02, DASH-01, DASH-02, DASH-03, USER-01, USER-02, USER-04]

must_haves:
  truths:
    - "Admin's selected router persists across browser refresh and syncs from API on page load"
    - "All API calls return typed, Zod-validated data matching the OpenAPI contract"
    - "TanStack Query hooks provide loading, error, and data states for routers, reports, and users"
    - "Router selection triggers query invalidation so dependent UI refreshes immediately"
  artifacts:
    - path: "website/src/stores/router-store.ts"
      provides: "Zustand store with selected router state and persistence"
      exports: ["useRouterStore"]
      contains: "selectedRouterId"
    - path: "website/src/lib/schemas/router.ts"
      provides: "Zod schemas for RouterResponse and router list"
      contains: "RouterResponseSchema"
    - path: "website/src/lib/schemas/user.ts"
      provides: "Zod schemas for UserResponse, CreateUserRequest, user list"
      contains: "UserResponseSchema"
    - path: "website/src/lib/schemas/report.ts"
      provides: "Zod schema for ReportSummary"
      contains: "ReportSummarySchema"
    - path: "website/src/api/router.ts"
      provides: "API functions for router list, selected, and select"
      exports: ["listRouters", "getSelectedRouter", "selectRouter"]
    - path: "website/src/api/report.ts"
      provides: "API function for report summary"
      exports: ["getReportSummary"]
    - path: "website/src/api/user.ts"
      provides: "API functions for user CRUD"
      exports: ["listUsers", "getUser", "createUser", "deleteUser"]
    - path: "website/src/hooks/use-routers.ts"
      provides: "TanStack Query hooks for router list and selection mutation"
      exports: ["useRouters", "useSelectRouter"]
    - path: "website/src/hooks/use-report-summary.ts"
      provides: "TanStack Query hook for report summary"
      exports: ["useReportSummary"]
    - path: "website/src/hooks/use-users.ts"
      provides: "TanStack Query hooks for user list, detail, create, and delete"
      exports: ["useUsers", "useUser", "useCreateUser", "useDeleteUser"]
  key_links:
    - from: "website/src/api/router.ts"
      to: "website/src/lib/axios/admin-client.ts"
      via: "adminClient.get/post for authenticated requests"
      pattern: "adminClient\\.(get|post)"
    - from: "website/src/hooks/use-routers.ts"
      to: "website/src/api/router.ts"
      via: "useQuery/useMutation wrapping listRouters, selectRouter"
      pattern: "listRouters|selectRouter"
    - from: "website/src/hooks/use-routers.ts"
      to: "website/src/stores/router-store.ts"
      via: "useSelectRouter mutation updates store on success and invalidates queries"
      pattern: "useRouterStore|invalidateQueries"
    - from: "website/src/hooks/use-users.ts"
      to: "website/src/api/user.ts"
      via: "useQuery/useMutation wrapping user API functions"
      pattern: "listUsers|createUser|deleteUser"
---

<objective>
Create the complete data layer that all subsequent Phase 2 plans depend on: router Zustand store, Zod schemas, typed API functions, and TanStack Query hooks for routers, reports, and users.

Purpose: Plans 02-05 (sidebar, router selector, dashboard, users) all need API data and React Query hooks. This plan establishes the shared data contracts and hooks in one place so executors never duplicate hook files or fight over shared state. Per D-01, D-02.

Output: Router store, Zod schemas, API functions, and TanStack Query hooks for routers, users, and reports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-dashboard-users/02-CONTEXT.md
@.planning/phases/02-layout-dashboard-users/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From website/src/stores/auth-store.ts (pattern to follow for router-store):
```typescript
export const useAuthStore = create<AuthStoreState>()(
  persist(
    (set) => ({
      // state + actions
    }),
    {
      name: 'mikmongo-auth',
      partialize: (state) => ({ /* only persist data fields */ }),
      onRehydrateStorage: () => (state) => { state?.setHydrated() },
    }
  )
)
```

From website/src/api/auth.ts (pattern to follow for API functions):
```typescript
import { adminClient } from '@/lib/axios/admin-client'
const parsed = SomeSchema.parse(response.data)
return parsed.data
```

From website/src/lib/schemas/auth.ts (pattern to follow for schemas):
```typescript
export const ApiResponseSchema = <T extends z.ZodTypeAny>(dataSchema: T) =>
  z.object({ success: z.boolean(), data: dataSchema })
```

From website/src/api/types.ts (extend with new types):
```typescript
export type ApiResponse<T> = { success: boolean; data: T }
```

From docs/openapi.docs.yml (OpenAPI contract):
```yaml
# RouterResponse (lines 537-574)
RouterResponse:
  properties:
    id: string (uuid), name: string, address: string, area: string,
    api_port: integer, rest_port: integer, username: string, use_ssl: boolean,
    is_master: boolean, is_active: boolean,
    status: string enum [online, offline, unknown],
    last_seen_at: string (date-time), notes: string,
    created_at: string (date-time), updated_at: string (date-time)

# UserResponse (lines 260-286)
UserResponse:
  properties:
    id: string (uuid), full_name: string, email: string, phone: string,
    role: string enum [superadmin, admin, cs, billing, technician, readonly],
    is_active: boolean, last_login: string (date-time, nullable),
    created_at: string (date-time), updated_at: string (date-time)

# CreateUserRequest (lines 243-258)
CreateUserRequest:
  required: [full_name, email, role, password]
  properties:
    full_name: string, email: string (format: email), phone: string,
    role: string enum [superadmin, admin, cs, billing, technician, readonly],
    password: string

# ReportSummary (lines 1511-1558)
ReportSummary:
  properties:
    period_start, period_end: string (date-time)
    total_revenue, total_invoiced: number (double)
    total_invoices, paid_invoices, unpaid_invoices, overdue_invoices: integer
    total_payments: integer
    total_customers, active_customers, new_customers: integer
    active_subscriptions, isolated_subscriptions, suspended_subscriptions: integer

# Endpoints:
GET  /api/v1/routers -> { success, data: RouterResponse[], meta: { total, limit, offset } }
GET  /api/v1/routers/selected -> { success, data: RouterResponse }
POST /api/v1/routers/select/{id} -> { success, data: RouterResponse }
GET  /api/v1/users?limit=&offset= -> { success, data: UserResponse[], meta }
POST /api/v1/users -> { success, data: UserResponse } (201)
GET  /api/v1/users/{id} -> { success, data: UserResponse }
DELETE /api/v1/users/{id} -> { success, data: { message: string } }
GET  /api/v1/reports/summary?from=&to= -> { success, data: ReportSummary }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zod schemas, router Zustand store, and typed API functions</name>
  <files>
    website/src/lib/schemas/router.ts
    website/src/lib/schemas/user.ts
    website/src/lib/schemas/report.ts
    website/src/stores/router-store.ts
    website/src/api/router.ts
    website/src/api/user.ts
    website/src/api/report.ts
    website/src/api/types.ts
  </files>
  <read_first>
    website/src/lib/schemas/auth.ts
    website/src/stores/auth-store.ts
    website/src/api/auth.ts
    website/src/api/types.ts
    website/src/lib/axios/admin-client.ts
    docs/openapi.docs.yml (lines 537-574 for RouterResponse, lines 260-286 for UserResponse, lines 1511-1558 for ReportSummary, lines 181-191 for Meta)
  </read_first>
  <action>
    Create the following files with exact Zod schemas matching the OpenAPI contract:

    **1. `website/src/lib/schemas/router.ts`**
    - `RouterResponseSchema`: z.object matching OpenAPI RouterResponse exactly:
      - id: z.string(), name: z.string(), address: z.string(), area: z.string(), api_port: z.number().optional(), rest_port: z.number().optional(), username: z.string(), use_ssl: z.boolean(), is_master: z.boolean(), is_active: z.boolean(), status: z.enum(['online', 'offline', 'unknown']), last_seen_at: z.string().nullable(), notes: z.string().optional(), created_at: z.string(), updated_at: z.string()
    - `RouterListResponseSchema`: ApiResponseSchema(z.array(RouterResponseSchema)).extend({ meta: MetaSchema }) where MetaSchema is z.object({ total: z.number(), limit: z.number(), offset: z.number() })
    - `SelectedRouterResponseSchema`: ApiResponseSchema(RouterResponseSchema)
    - Export inferred types: RouterResponse, RouterListData

    **2. `website/src/lib/schemas/user.ts`**
    - `UserResponseSchema`: z.object matching OpenAPI UserResponse exactly:
      - id: z.string(), full_name: z.string(), email: z.string().email(), phone: z.string().optional(), role: z.enum(['superadmin', 'admin', 'cs', 'billing', 'technician', 'readonly']), is_active: z.boolean(), last_login: z.string().nullable(), created_at: z.string(), updated_at: z.string()
    - `CreateUserRequestSchema`: z.object matching OpenAPI:
      - full_name: z.string().min(1, 'Full name is required'), email: z.string().min(1, 'Email is required').email('Please enter a valid email address'), phone: z.string().optional(), role: z.enum(['superadmin', 'admin', 'cs', 'billing', 'technician', 'readonly'], { required_error: 'Please select a role' }), password: z.string().min(8, 'Password must be at least 8 characters')
    - `CreateUserFormSchema`: extend CreateUserRequestSchema with confirm_password: z.string().min(1), then .refine() that password === confirm_password with message 'Passwords do not match' and path ['confirm_password']
    - `UserListResponseSchema`: ApiResponseSchema(z.array(UserResponseSchema)).extend({ meta: MetaSchema })
    - Export inferred types: UserResponse, CreateUserRequest, CreateUserFormValues

    **3. `website/src/lib/schemas/report.ts`**
    - `ReportSummarySchema`: z.object matching OpenAPI ReportSummary exactly:
      - period_start: z.string(), period_end: z.string(), total_revenue: z.number(), total_invoiced: z.number(), total_invoices: z.number(), paid_invoices: z.number(), unpaid_invoices: z.number(), overdue_invoices: z.number(), total_payments: z.number(), total_customers: z.number(), active_customers: z.number(), new_customers: z.number(), active_subscriptions: z.number(), isolated_subscriptions: z.number(), suspended_subscriptions: z.number()
    - `ReportSummaryResponseSchema`: ApiResponseSchema(ReportSummarySchema)
    - Export inferred type: ReportSummary

    **4. `website/src/stores/router-store.ts`** -- per D-01 (separate store, not auth slice) and D-02 (syncs from API on load)
    - Create Zustand store following the same pattern as auth-store.ts (persist + partialize + hydration gate):
    ```
    State fields: selectedRouterId: string | null, selectedRouterName: string | null, isHydrated: boolean
    Actions: setSelectedRouter(id: string, name: string): void, clearSelectedRouter(): void, setHydrated(): void
    ```
    - persist config: name: 'mikmongo-router', partialize to only { selectedRouterId, selectedRouterName }
    - onRehydrateStorage: call setHydrated()
    - Do NOT auto-fetch in the store -- consuming components call API and then sync store per D-02

    **5. Update `website/src/api/types.ts`**
    - Add type exports for the new schemas:
      - `export type RouterResponse = z.infer<typeof RouterResponseSchema>` (import from schemas/router)
      - `export type UserResponse = z.infer<typeof UserResponseSchema>` (import from schemas/user)
      - `export type ReportSummary = z.infer<typeof ReportSummarySchema>` (import from schemas/report)
      - `export type CreateUserRequest = z.infer<typeof CreateUserRequestSchema>`
      - `export type CreateUserFormValues = z.infer<typeof CreateUserFormSchema>`
    - Add Meta type export
    - Do NOT remove existing type exports

    **6. `website/src/api/router.ts`** -- following auth.ts pattern
    - `listRouters(limit?: number, offset?: number)`: GET /routers with query params, parse with RouterListResponseSchema, return { routers: parsed.data, meta: parsed.meta }
    - `getSelectedRouter()`: GET /routers/selected, parse with SelectedRouterResponseSchema, return parsed.data
    - `selectRouter(id: string)`: POST /routers/select/{id}, parse with SelectedRouterResponseSchema, return parsed.data
    - All use adminClient

    **7. `website/src/api/user.ts`** -- following auth.ts pattern
    - `listUsers(limit: number, offset: number)`: GET /users?limit={limit}&offset={offset}, parse with UserListResponseSchema, return { users: parsed.data, meta: parsed.meta }
    - `getUser(id: string)`: GET /users/{id}, parse with ApiResponseSchema(UserResponseSchema), return parsed.data
    - `createUser(data: CreateUserRequest)`: POST /users with data body, parse with ApiResponseSchema(UserResponseSchema), return parsed.data
    - `deleteUser(id: string)`: DELETE /users/{id}, parse with MessageResponseSchema, return parsed.data
    - All use adminClient

    **8. `website/src/api/report.ts`** -- following auth.ts pattern
    - `getReportSummary(from?: string, to?: string)`: GET /reports/summary with optional query params, parse with ReportSummaryResponseSchema, return parsed.data
    - Use adminClient
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - website/src/lib/schemas/router.ts exports RouterResponseSchema, RouterListResponseSchema, SelectedRouterResponseSchema
    - website/src/lib/schemas/user.ts exports UserResponseSchema, CreateUserRequestSchema, CreateUserFormSchema, UserListResponseSchema
    - website/src/lib/schemas/report.ts exports ReportSummarySchema, ReportSummaryResponseSchema
    - website/src/stores/router-store.ts exports useRouterStore with selectedRouterId, selectedRouterName, setSelectedRouter, clearSelectedRouter
    - website/src/api/router.ts exports listRouters, getSelectedRouter, selectRouter
    - website/src/api/user.ts exports listUsers, getUser, createUser, deleteUser
    - website/src/api/report.ts exports getReportSummary
    - website/src/api/types.ts has type exports for RouterResponse, UserResponse, ReportSummary, CreateUserRequest, CreateUserFormValues
    - TypeScript compilation passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TanStack Query hooks for routers, reports, and users</name>
  <files>
    website/src/hooks/use-routers.ts
    website/src/hooks/use-report-summary.ts
    website/src/hooks/use-users.ts
  </files>
  <read_first>
    website/src/api/router.ts (created in Task 1)
    website/src/api/user.ts (created in Task 1)
    website/src/api/report.ts (created in Task 1)
    website/src/stores/router-store.ts (created in Task 1)
    website/src/lib/axios/admin-client.ts
  </read_first>
  <action>
    Create TanStack Query hooks that all downstream plans will import. This is the ONLY place these hooks are defined -- no other plan should create them.

    **1. `website/src/hooks/use-routers.ts`**
    - Import useQuery, useMutation, useQueryClient from @tanstack/react-query
    - Import listRouters, selectRouter from @/api/router
    - Import useRouterStore from @/stores/router-store
    - `useRouters()`: useQuery with queryKey ['routers'], queryFn: () => listRouters(), staleTime 2 * 60 * 1000. Returns { data: { routers, meta }, isLoading, error, refetch }
    - `useSelectRouter()`: useMutation with mutationFn: (id: string) => selectRouter(id). onSuccess receives the RouterResponse and:
      1. Calls useRouterStore.getState().setSelectedRouter(id, response.name)
      2. Calls queryClient.invalidateQueries({ queryKey: ['routers'] }) per D-04 (dashboard refreshes on router switch)
      3. Calls queryClient.invalidateQueries({ queryKey: ['report-summary'] }) so KPI widgets refresh
    - Export both hooks

    **2. `website/src/hooks/use-report-summary.ts`**
    - Import useQuery from @tanstack/react-query
    - Import getReportSummary from @/api/report
    - `useReportSummary(from?: string, to?: string)`: useQuery with queryKey ['report-summary', from, to], queryFn: () => getReportSummary(from, to), staleTime 5 * 60 * 1000. Returns { data, isLoading, error, refetch }
    - Export the hook

    **3. `website/src/hooks/use-users.ts`**
    - Import useQuery, useMutation, useQueryClient from @tanstack/react-query
    - Import listUsers, getUser, createUser, deleteUser from @/api/user
    - Import CreateUserRequest type from @/api/types
    - `useUsers(limit: number, offset: number)`: useQuery with queryKey ['users', limit, offset], queryFn: () => listUsers(limit, offset), staleTime 2 * 60 * 1000. Returns { data: { users, meta }, isLoading, error, refetch }
    - `useUser(id: string)`: useQuery with queryKey ['users', id], queryFn: () => getUser(id), enabled: !!id. Returns { data, isLoading, error }
    - `useCreateUser()`: useMutation with mutationFn: (data: CreateUserRequest) => createUser(data). onSuccess: invalidate ['users']. Returns { mutate, mutateAsync, isPending }
    - `useDeleteUser()`: useMutation with mutationFn: (id: string) => deleteUser(id). onSuccess: invalidate ['users']. Returns { mutate, mutateAsync, isPending }
    - Export all four hooks
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - website/src/hooks/use-routers.ts exports useRouters and useSelectRouter
    - useSelectRouter mutation updates router store AND invalidates both ['routers'] and ['report-summary'] queries on success
    - website/src/hooks/use-report-summary.ts exports useReportSummary with ['report-summary'] query key
    - website/src/hooks/use-users.ts exports useUsers, useUser, useCreateUser, useDeleteUser
    - useCreateUser and useDeleteUser invalidate ['users'] query on success
    - TypeScript compilation passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `cd website && npx tsc --noEmit` passes with zero errors
2. All schema files exist with correct exports: `grep -c "export" website/src/lib/schemas/router.ts website/src/lib/schemas/user.ts website/src/lib/schemas/report.ts`
3. Router store has persist + partialize: `grep "partialize" website/src/stores/router-store.ts`
4. API files use adminClient: `grep "adminClient" website/src/api/router.ts website/src/api/user.ts website/src/api/report.ts`
5. Hooks exist with correct query keys: `grep "queryKey" website/src/hooks/use-routers.ts website/src/hooks/use-report-summary.ts website/src/hooks/use-users.ts`
6. useSelectRouter invalidates queries: `grep "invalidateQueries" website/src/hooks/use-routers.ts`
</verification>

<success_criteria>
- All 11 files created/modified exist with correct exports
- Zod schemas match OpenAPI contract exactly (field names, types, enums)
- Router store follows auth-store pattern (persist, partialize, hydration)
- API functions follow auth.ts pattern (adminClient, Zod parse, return typed data)
- TanStack Query hooks provide loading/error/data states for routers, reports, users
- useSelectRouter updates store AND invalidates dependent queries per D-04
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-dashboard-users/02-01-SUMMARY.md`
</output>
