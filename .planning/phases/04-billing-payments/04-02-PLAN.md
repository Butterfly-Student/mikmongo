---
phase: 04-billing-payments
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - website/src/features/billing/invoices/data/schema.ts
  - website/src/features/billing/invoices/data/columns.tsx
  - website/src/features/billing/invoices/components/invoice-table.tsx
  - website/src/features/billing/invoices/components/invoice-detail-sheet.tsx
  - website/src/features/billing/invoices/components/invoice-generation-trigger.tsx
  - website/src/features/billing/invoices/index.tsx
  - website/src/routes/_authenticated/invoices/index.tsx
  - website/src/components/layout/data/sidebar-data.ts
autonomous: true
requirements: [INV-01, INV-02, INV-03]

must_haves:
  truths:
    - "Admin can view a paginated invoice list with search and faceted status/overdue filters"
    - "Admin can click an invoice row to open a side sheet showing full details and linked payments"
    - "Admin can trigger monthly invoice generation via a button with confirmation dialog"
  artifacts:
    - path: "website/src/features/billing/invoices/index.tsx"
      provides: "Invoice management page component"
      contains: "Manajemen Tagihan"
    - path: "website/src/features/billing/invoices/components/invoice-detail-sheet.tsx"
      provides: "Side sheet for invoice details with linked payments"
      contains: "Detail Tagihan"
    - path: "website/src/features/billing/invoices/components/invoice-generation-trigger.tsx"
      provides: "Monthly generation button with confirm dialog"
      contains: "Buat Tagihan Bulanan"
    - path: "website/src/routes/_authenticated/invoices/index.tsx"
      provides: "Route file for /invoices"
      contains: "createFileRoute"
  key_links:
    - from: "website/src/features/billing/invoices/index.tsx"
      to: "website/src/hooks/use-invoices.ts"
      via: "useInvoices hook"
      pattern: "import.*useInvoices.*from.*@/hooks/use-invoices"
    - from: "website/src/features/billing/invoices/components/invoice-detail-sheet.tsx"
      to: "website/src/hooks/use-invoices.ts"
      via: "useInvoice hook for detail fetch"
      pattern: "import.*useInvoice.*from.*@/hooks/use-invoices"
    - from: "website/src/routes/_authenticated/invoices/index.tsx"
      to: "website/src/features/billing/invoices/index.tsx"
      via: "component import"
      pattern: "import.*from.*@/features/billing/invoices"
---

<objective>
Build the admin invoice management page with DataTable, faceted filters, invoice detail side sheet, and monthly generation trigger.

Purpose: Admin can view, filter, and inspect invoices — the core billing visibility requirement.
Output: Complete invoice list page at `/invoices` with detail sheet and generation trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-billing-payments/04-CONTEXT.md
@.planning/phases/04-billing-payments/04-UI-SPEC.md
@.planning/phases/04-billing-payments/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From website/src/lib/schemas/billing.ts:
```typescript
export type InvoiceResponse = z.infer<typeof invoiceResponseSchema>
// status: 'draft' | 'sent' | 'unpaid' | 'partial' | 'paid' | 'overpaid' | 'overdue' | 'cancelled' | 'refunded'
// invoice_type: 'recurring' | 'installation' | 'additional' | 'refund'
```

From website/src/hooks/use-invoices.ts:
```typescript
export function useInvoices(limit?: number, offset?: number)
export function useOverdueInvoices()
export function useInvoice(id: string | null)
export function useTriggerMonthlyBilling()
```

<!-- Existing reusable components -->
From website/src/components/data-table/:
DataTable, DataTableToolbar, DataTableFacetedFilter, DataTableColumnHeader, DataTablePagination

From website/src/components/confirm-dialog.tsx:
```typescript
export function ConfirmDialog(props: {
  open: boolean; onOpenChange: (open: boolean) => void;
  title: React.ReactNode; desc: React.JSX.Element | string;
  cancelBtnText?: string; confirmText?: React.ReactNode;
  destructive?: boolean; handleConfirm: () => void;
  isLoading?: boolean; children?: React.ReactNode;
})
```

From website/src/components/ui/sheet.tsx:
Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invoice feature module (schema, columns, table, detail sheet, generation trigger)</name>
  <files>
    website/src/features/billing/invoices/data/schema.ts,
    website/src/features/billing/invoices/data/columns.tsx,
    website/src/features/billing/invoices/components/invoice-table.tsx,
    website/src/features/billing/invoices/components/invoice-detail-sheet.tsx,
    website/src/features/billing/invoices/components/invoice-generation-trigger.tsx,
    website/src/features/billing/invoices/index.tsx
  </files>
  <read_first>
    website/src/features/subscriptions/data/schema.ts,
    website/src/features/subscriptions/data/columns.tsx,
    website/src/features/subscriptions/components/subscription-table.tsx,
    website/src/features/subscriptions/index.tsx,
    website/src/features/customers/data/columns.tsx,
    website/src/components/data-table/data-table.tsx,
    website/src/components/data-table/data-table-toolbar.tsx,
    website/src/components/data-table/data-table-faceted-filter.tsx,
    website/src/components/ui/sheet.tsx,
    website/src/components/confirm-dialog.tsx,
    website/src/lib/schemas/billing.ts,
    website/src/hooks/use-invoices.ts
  </read_first>
  <action>
**Per D-01:** Invoice detail opens in a side sheet (right-side panel). **Per D-02:** Overdue filter uses faceted filter dropdown.

**1. `features/billing/invoices/data/schema.ts`:**
Re-export `InvoiceResponse` from `@/lib/schemas/billing` and define UI-specific constants:
- `invoiceStatuses` array: `[{ value: 'paid', label: 'Lunas', className: 'bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300' }, { value: 'unpaid', label: 'Belum Lunas', className: 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900 dark:text-yellow-300' }, { value: 'overdue', label: 'Terlambat', className: 'bg-red-100 text-red-700 dark:bg-red-900 dark:text-red-300' }, { value: 'partial', label: 'Sebagian', className: 'bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300' }, { value: 'draft', label: 'Draft', className: 'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-300' }, { value: 'cancelled', label: 'Dibatalkan', className: 'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-300' }, { value: 'refunded', label: 'Dikembalikan', className: 'bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300' }]`
- `overdueOptions` array: `[{ value: 'yes', label: 'Terlambat' }, { value: 'no', label: 'Belum Terlambat' }]`

**2. `features/billing/invoices/data/columns.tsx`:**
Define TanStack Table `ColumnDef<InvoiceResponse>[]`:
- `invoice_number`: header "No. Tagihan" (sortable), cell `font-mono text-xs`
- `customer_id`: header "Pelanggan" (sortable) -- render customer_id for now (will be enriched when API returns customer name)
- `issue_date`: header "Tanggal" (sortable), cell formatted `dd/MM/yyyy` using `new Date(value).toLocaleDateString('id-ID')`
- `payment_deadline`: header "Jatuh Tempo" (sortable), cell formatted `dd/MM/yyyy`, add `text-red-600` class if `new Date(row.original.payment_deadline) < new Date()` and status is not 'paid'
- `total_amount`: header "Total" (sortable), cell right-aligned, formatted `Rp ${amount.toLocaleString('id-ID')}` (Indonesian locale, period as thousands separator)
- `status`: header "Status", cell renders Badge with className from `invoiceStatuses` lookup, label from same lookup

**3. `features/billing/invoices/components/invoice-table.tsx`:**
Follow subscription-table.tsx pattern:
- Receives `data: InvoiceResponse[]` prop
- Uses `useReactTable` with columns from columns.tsx
- Renders `DataTable` with `DataTableToolbar` containing:
  - Search input with placeholder "Cari nomor tagihan atau pelanggan..."
  - `DataTableFacetedFilter` for "Status" column using `invoiceStatuses`
  - Column visibility toggle
- `DataTablePagination` footer
- `onRowClick` prop that fires when any row is clicked (passes InvoiceResponse)

**4. `features/billing/invoices/components/invoice-detail-sheet.tsx`:**
Props: `{ invoice: InvoiceResponse | null; open: boolean; onOpenChange: (open: boolean) => void }`
Uses Sheet component with `className="w-full sm:max-w-[540px]"`:
- SheetHeader: "Detail Tagihan #{invoice.invoice_number}"
- Section 1 "Info Tagihan": customer_id, billing_period_start to billing_period_end, issue_date, due_date, invoice_type — displayed as label/value pairs in `grid grid-cols-2 gap-4`
- Section 2 "Jumlah": subtotal, tax_amount (if > 0), discount_amount (if > 0), late_fee (if > 0), total_amount — all Rp formatted. Total in `text-xl font-bold`
- Section 3 "Status": status badge inline
- Section 4 "Riwayat Pembayaran": placeholder text "Lihat halaman pembayaran untuk detail" (per UI-SPEC the sheet shows linked payments, but the API does not embed payments in invoice response; a future enhancement can add this)
- SheetFooter: Button "Tutup" onClick -> onOpenChange(false)

**5. `features/billing/invoices/components/invoice-generation-trigger.tsx`:**
- Button labeled "Buat Tagihan Bulanan" with FileText icon
- On click opens ConfirmDialog:
  - title: "Buat Tagihan Bulanan?"
  - desc: "Tindakan ini akan membuat tagihan untuk semua pelanggan aktif bulan ini. Lanjutkan?"
  - confirmText: "Buat Tagihan"
  - cancelBtnText: "Batal"
  - handleConfirm calls `useTriggerMonthlyBilling().mutate()`
  - isLoading from mutation isPending

**6. `features/billing/invoices/index.tsx`:**
Main page component:
- Page header: title "Manajemen Tagihan", subtitle "Kelola tagihan pelanggan dan buat tagihan bulanan"
- Right slot: `<InvoiceGenerationTrigger />`
- Uses `useInvoices()` hook
- Loading: skeleton rows (6 rows, pulsing animate-pulse)
- Empty: "Tidak ada tagihan ditemukan." with FileText icon (from lucide-react), muted text
- Data: renders `<InvoiceTable data={invoices} onRowClick={handleRowClick} />`
- State for selected invoice + sheet open
- Renders `<InvoiceDetailSheet invoice={selectedInvoice} open={sheetOpen} onOpenChange={setSheetOpen} />`
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - website/src/features/billing/invoices/index.tsx contains `Manajemen Tagihan`
    - website/src/features/billing/invoices/index.tsx contains `useInvoices`
    - website/src/features/billing/invoices/index.tsx contains `InvoiceDetailSheet`
    - website/src/features/billing/invoices/components/invoice-detail-sheet.tsx contains `Detail Tagihan`
    - website/src/features/billing/invoices/components/invoice-detail-sheet.tsx contains `sm:max-w-[540px]`
    - website/src/features/billing/invoices/components/invoice-generation-trigger.tsx contains `Buat Tagihan Bulanan`
    - website/src/features/billing/invoices/components/invoice-generation-trigger.tsx contains `useTriggerMonthlyBilling`
    - website/src/features/billing/invoices/data/columns.tsx contains `invoice_number`
    - website/src/features/billing/invoices/data/columns.tsx contains `toLocaleString`
    - website/src/features/billing/invoices/data/schema.ts contains `invoiceStatuses`
  </acceptance_criteria>
  <done>Invoice management page renders at /invoices with paginated table, faceted status filter, clickable rows opening detail sheet, and monthly generation trigger button with confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Create invoice route and enable sidebar nav</name>
  <files>
    website/src/routes/_authenticated/invoices/index.tsx,
    website/src/components/layout/data/sidebar-data.ts
  </files>
  <read_first>
    website/src/routes/_authenticated/subscriptions/index.tsx,
    website/src/routes/_authenticated/customers/index.tsx,
    website/src/components/layout/data/sidebar-data.ts
  </read_first>
  <action>
**1. Create `website/src/routes/_authenticated/invoices/index.tsx`:**
Follow the exact pattern from subscriptions route:
```typescript
import { createFileRoute } from '@tanstack/react-router'
import InvoicesPage from '@/features/billing/invoices'

export const Route = createFileRoute('/_authenticated/invoices/')({
  component: InvoicesPage,
})
```

**2. Update `website/src/components/layout/data/sidebar-data.ts`:**
In the "Billing" nav group, change the Invoices item:
- `url: '#'` -> `url: '/invoices'`
- Remove `disabled: true`
Keep Payments and Cash items still disabled (they get enabled in Plans 03 and 04).
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - website/src/routes/_authenticated/invoices/index.tsx contains `createFileRoute('/_authenticated/invoices/')`
    - website/src/routes/_authenticated/invoices/index.tsx contains `import InvoicesPage from '@/features/billing/invoices'`
    - website/src/components/layout/data/sidebar-data.ts contains `url: '/invoices'` (for Invoices item)
    - website/src/components/layout/data/sidebar-data.ts does NOT contain `disabled: true` on the Invoices item line
    - website/src/components/layout/data/sidebar-data.ts still contains `disabled: true` for Payments and Cash items
  </acceptance_criteria>
  <done>Invoice route registered at /invoices, sidebar Invoices link enabled while Payments and Cash remain disabled</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Invoice feature directory exists at `website/src/features/billing/invoices/` with correct sub-structure
- Route file creates TanStack Router route at `/_authenticated/invoices/`
- Sidebar data has Invoices enabled at `/invoices`
</verification>

<success_criteria>
- Admin can navigate to /invoices from sidebar
- Invoice list page shows with DataTable, search, faceted status filter
- Clicking a row opens InvoiceDetailSheet with invoice details
- "Buat Tagihan Bulanan" button triggers monthly generation with confirmation dialog
- All Indonesian labels match UI-SPEC copywriting
</success_criteria>

<output>
After completion, create `.planning/phases/04-billing-payments/04-02-SUMMARY.md`
</output>
