---
phase: 04-billing-payments
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - website/src/features/billing/cash/data/schema.ts
  - website/src/features/billing/cash/data/columns.tsx
  - website/src/features/billing/cash/components/cash-table.tsx
  - website/src/features/billing/cash/components/create-cash-entry-dialog.tsx
  - website/src/features/billing/cash/components/cash-reject-dialog.tsx
  - website/src/features/billing/cash/components/petty-cash-card.tsx
  - website/src/features/billing/cash/components/top-up-dialog.tsx
  - website/src/features/billing/cash/index.tsx
  - website/src/routes/_authenticated/cash/index.tsx
  - website/src/components/layout/data/sidebar-data.ts
autonomous: true
requirements: [CASH-01, CASH-02, CASH-03, CASH-04]

must_haves:
  truths:
    - "Admin can view cash entries in a table with status and type faceted filters"
    - "Admin can create new cash entry via a dialog form"
    - "Admin can inline-approve pending cash entries with a single click (no dialog)"
    - "Admin can reject pending cash entries with a reason dialog"
    - "Admin can view petty cash fund balance and top up via dialog"
  artifacts:
    - path: "website/src/features/billing/cash/index.tsx"
      provides: "Cash management page with petty cash section"
      contains: "Kas & Dana Kecil"
    - path: "website/src/features/billing/cash/components/petty-cash-card.tsx"
      provides: "Balance card with top-up button"
      contains: "Saldo Dana Kecil Saat Ini"
    - path: "website/src/features/billing/cash/components/create-cash-entry-dialog.tsx"
      provides: "Cash entry creation form"
      contains: "Tambah Entri Kas"
    - path: "website/src/features/billing/cash/components/cash-reject-dialog.tsx"
      provides: "Rejection dialog with reason textarea"
      contains: "Tolak Entri Kas"
    - path: "website/src/routes/_authenticated/cash/index.tsx"
      provides: "Route for /cash"
      contains: "createFileRoute"
  key_links:
    - from: "website/src/features/billing/cash/index.tsx"
      to: "website/src/hooks/use-cash.ts"
      via: "useCashEntries + usePettyCashFunds hooks"
      pattern: "import.*useCashEntries.*from.*@/hooks/use-cash"
    - from: "website/src/features/billing/cash/data/columns.tsx"
      to: "website/src/hooks/use-cash.ts"
      via: "useApproveCashEntry for inline approve"
      pattern: "useApproveCashEntry"
---

<objective>
Build the admin cash management page with cash entries table (inline approve, reject dialog), petty cash balance card with top-up, and create cash entry dialog.

Purpose: Admin can manage cash flow with approval workflow and petty cash fund.
Output: Complete cash page at `/cash` with entries table and petty cash section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-billing-payments/04-CONTEXT.md
@.planning/phases/04-billing-payments/04-UI-SPEC.md
@.planning/phases/04-billing-payments/04-01-SUMMARY.md

<interfaces>
From website/src/lib/schemas/billing.ts:
```typescript
export type CashEntryResponse = z.infer<typeof cashEntryResponseSchema>
// type: 'income' | 'expense'
// source: 'invoice' | 'agent_invoice' | 'installation' | 'penalty' | 'other' | 'operational' | 'upstream' | 'purchase' | 'salary'
export type CreateCashEntry = z.infer<typeof createCashEntrySchema>
export type PettyCashFundResponse = z.infer<typeof pettyCashFundResponseSchema>
export const createCashEntrySchema
export const topUpFundSchema
```

From website/src/hooks/use-cash.ts:
```typescript
export function useCashEntries(params?: { type?: string; status?: string; limit?: number; offset?: number })
export function useCreateCashEntry()
export function useApproveCashEntry()   // mutationFn: (id: string)
export function useRejectCashEntry()    // mutationFn: { id, reason }
export function usePettyCashFunds()
export function useCreatePettyCashFund()
export function useTopUpPettyCashFund() // mutationFn: { id, amount }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cash feature module (schema, columns, table, dialogs, petty cash card)</name>
  <files>
    website/src/features/billing/cash/data/schema.ts,
    website/src/features/billing/cash/data/columns.tsx,
    website/src/features/billing/cash/components/cash-table.tsx,
    website/src/features/billing/cash/components/create-cash-entry-dialog.tsx,
    website/src/features/billing/cash/components/cash-reject-dialog.tsx,
    website/src/features/billing/cash/components/petty-cash-card.tsx,
    website/src/features/billing/cash/components/top-up-dialog.tsx,
    website/src/features/billing/cash/index.tsx
  </files>
  <read_first>
    website/src/features/billing/invoices/data/schema.ts,
    website/src/features/billing/invoices/data/columns.tsx,
    website/src/features/billing/invoices/components/invoice-table.tsx,
    website/src/features/billing/invoices/index.tsx,
    website/src/features/billing/payments/components/reject-payment-dialog.tsx,
    website/src/features/customers/components/create-customer-dialog.tsx,
    website/src/components/confirm-dialog.tsx,
    website/src/components/ui/card.tsx,
    website/src/lib/schemas/billing.ts,
    website/src/hooks/use-cash.ts
  </read_first>
  <action>
**Per D-07:** Inline approve (single-click button, no dialog). Reject opens dialog with reason field. **Per D-08:** Single table with faceted filter by status and type. **Per D-09:** Petty cash fund displayed as a section within the cash page, not a separate page.

**1. `features/billing/cash/data/schema.ts`:**
Re-export from `@/lib/schemas/billing`. Define:
- `cashEntryStatuses`: `[{ value: 'pending', label: 'Menunggu', className: 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900 dark:text-yellow-300' }, { value: 'approved', label: 'Disetujui', className: 'bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300' }, { value: 'rejected', label: 'Ditolak', className: 'bg-red-100 text-red-700 dark:bg-red-900 dark:text-red-300' }]`
- `cashEntryTypes`: `[{ value: 'income', label: 'Masuk', className: 'bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300' }, { value: 'expense', label: 'Keluar', className: 'bg-red-100 text-red-700 dark:bg-red-900 dark:text-red-300' }]`
- `cashEntrySources`: `[{ value: 'invoice', label: 'Tagihan' }, { value: 'agent_invoice', label: 'Tagihan Agen' }, { value: 'installation', label: 'Instalasi' }, { value: 'penalty', label: 'Denda' }, { value: 'other', label: 'Lainnya' }, { value: 'operational', label: 'Operasional' }, { value: 'upstream', label: 'Upstream' }, { value: 'purchase', label: 'Pembelian' }, { value: 'salary', label: 'Gaji' }]`

**2. `features/billing/cash/data/columns.tsx`:**
`ColumnDef<CashEntryResponse>[]` — the last column includes inline action buttons:
- `entry_date`: header "Tanggal" (sortable), `dd/MM/yyyy`
- `type`: header "Tipe" (not sortable), Badge: "Masuk" green / "Keluar" red from `cashEntryTypes`
- `source`: header "Sumber" (not sortable)
- `description`: header "Deskripsi" (not sortable), truncate at 40 chars with `title` attr for full text
- `amount`: header "Jumlah" (sortable), `Rp ${amount.toLocaleString('id-ID')}`
- `status`: header "Status", Badge from `cashEntryStatuses`
- `actions`: header "Aksi", renders inline buttons ONLY when `row.original.status === 'pending'`:
  - Green CheckIcon button (`text-green-600 h-8 w-8`) with tooltip "Setujui" — onClick calls `onApprove(row.original.id)` (passed via table meta)
  - Red XIcon button (`text-red-600 h-8 w-8`) with tooltip "Tolak" — onClick calls `onReject(row.original)` (passed via table meta)
  - For non-pending rows: render nothing in Aksi column

**3. `features/billing/cash/components/cash-table.tsx`:**
DataTable wrapper:
- Receives `data: CashEntryResponse[]`, `onApprove: (id: string) => void`, `onReject: (entry: CashEntryResponse) => void`
- Passes `onApprove` and `onReject` via TanStack Table `meta` option so columns can access them
- DataTableToolbar with:
  - Search input placeholder "Cari deskripsi atau sumber..."
  - DataTableFacetedFilter for "status" using `cashEntryStatuses`
  - DataTableFacetedFilter for "type" using `cashEntryTypes`

**4. `features/billing/cash/components/create-cash-entry-dialog.tsx`:**
Dialog with form using react-hook-form + zodResolver with `createCashEntrySchema`:
- Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`
- Form fields:
  - `type`: Select dropdown, options: Masuk (income) / Keluar (expense)
  - `source`: Select dropdown, options from `cashEntrySources`
  - `amount`: Input type number, label "Jumlah (Rp)"
  - `description`: Textarea, label "Deskripsi"
  - `payment_method`: Input text, label "Metode Pembayaran"
  - `bank_name`: Input text, label "Nama Bank" (optional)
  - `account_number`: Input text, label "No. Rekening" (optional)
  - `entry_date`: Input type date, label "Tanggal" (optional, defaults to today)
  - `notes`: Textarea, label "Catatan" (optional)
- Submit calls `useCreateCashEntry().mutate(data)`, closes dialog on success
- Buttons: "Batal" (ghost) + "Simpan" (default)

**5. `features/billing/cash/components/cash-reject-dialog.tsx`:**
AlertDialog with Textarea (same pattern as reject-payment-dialog):
- title: "Tolak Entri Kas"
- desc: "Berikan alasan penolakan untuk entri ini."
- Textarea: label "Alasan", placeholder "Masukkan alasan penolakan...", required
- Buttons: "Batal" (ghost) + "Tolak" (destructive)
- handleConfirm calls `useRejectCashEntry().mutate({ id: entry.id, reason })`
- Disable submit if reason is empty

**6. `features/billing/cash/components/petty-cash-card.tsx`:**
Props: `{ fund: PettyCashFundResponse | null; onTopUp: () => void; onCreate: () => void }`
Card component (`bg-card border rounded-lg p-6`):
- If `fund` is null: "Dana kecil belum dikonfigurasi. Buat dana kecil baru." with "Buat Dana Kecil" button calling `onCreate`
- If `fund` exists:
  - Left side: label "Saldo Dana Kecil Saat Ini" (`text-sm text-muted-foreground`), balance `Rp ${fund.current_balance.toLocaleString('id-ID')}` (`text-3xl font-bold`)
  - Right side: "Tambah Saldo" button calling `onTopUp`
  - Responsive: stacks vertically on `< md` (flex-col md:flex-row)

**7. `features/billing/cash/components/top-up-dialog.tsx`:**
Dialog:
- title: "Tambah Saldo Dana Kecil"
- desc: "Masukkan jumlah dan keterangan penambahan saldo."
- Input type number for amount, label "Jumlah (Rp)", required
- Buttons: "Batal" + "Tambah Saldo"
- handleConfirm calls `useTopUpPettyCashFund().mutate({ id: fund.id, amount })`

**8. `features/billing/cash/index.tsx`:**
Main page:
- Page header: title "Kas & Dana Kecil", subtitle "Kelola entri kas dan saldo dana kecil"
- Right slot: "Tambah Entri Kas" button opens CreateCashEntryDialog
- Section 1: `<PettyCashCard fund={funds?.[0] ?? null} onTopUp={openTopUp} onCreate={openCreate} />`
- `space-y-6` gap between PettyCashCard and table section
- Section 2: CashTable with data from `useCashEntries()`
- Loading: skeleton rows
- Empty: "Tidak ada entri kas ditemukan." with Banknote icon
- State for: createOpen, rejectTarget (CashEntryResponse | null), topUpOpen
- onApprove handler calls `useApproveCashEntry().mutate(id)` directly (no dialog, per D-07)
- onReject handler sets rejectTarget (opens CashRejectDialog)
- Renders: CreateCashEntryDialog, CashRejectDialog, TopUpDialog
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - website/src/features/billing/cash/index.tsx contains `Kas & Dana Kecil`
    - website/src/features/billing/cash/index.tsx contains `useCashEntries`
    - website/src/features/billing/cash/index.tsx contains `usePettyCashFunds`
    - website/src/features/billing/cash/index.tsx contains `useApproveCashEntry`
    - website/src/features/billing/cash/components/petty-cash-card.tsx contains `Saldo Dana Kecil Saat Ini`
    - website/src/features/billing/cash/components/petty-cash-card.tsx contains `Tambah Saldo`
    - website/src/features/billing/cash/components/create-cash-entry-dialog.tsx contains `createCashEntrySchema`
    - website/src/features/billing/cash/components/cash-reject-dialog.tsx contains `Tolak Entri Kas`
    - website/src/features/billing/cash/components/cash-reject-dialog.tsx contains `useRejectCashEntry`
    - website/src/features/billing/cash/components/top-up-dialog.tsx contains `Tambah Saldo Dana Kecil`
    - website/src/features/billing/cash/components/top-up-dialog.tsx contains `useTopUpPettyCashFund`
    - website/src/features/billing/cash/data/columns.tsx contains `CheckIcon` or `Check`
    - website/src/features/billing/cash/data/schema.ts contains `cashEntryStatuses`
  </acceptance_criteria>
  <done>Cash page has entries table with inline approve/reject, faceted filters for status and type, create dialog with full form, petty cash card with balance and top-up. All Indonesian labels match UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Create cash route and enable sidebar nav</name>
  <files>
    website/src/routes/_authenticated/cash/index.tsx,
    website/src/components/layout/data/sidebar-data.ts
  </files>
  <read_first>
    website/src/routes/_authenticated/invoices/index.tsx,
    website/src/components/layout/data/sidebar-data.ts
  </read_first>
  <action>
**1. Create `website/src/routes/_authenticated/cash/index.tsx`:**
```typescript
import { createFileRoute } from '@tanstack/react-router'
import CashPage from '@/features/billing/cash'

export const Route = createFileRoute('/_authenticated/cash/')({
  component: CashPage,
})
```

**2. Update `website/src/components/layout/data/sidebar-data.ts`:**
In the "Billing" nav group, change the Cash item:
- `url: '#'` -> `url: '/cash'`
- Remove `disabled: true`
All three Billing items should now be enabled: Invoices at `/invoices`, Payments at `/payments`, Cash at `/cash`.
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - website/src/routes/_authenticated/cash/index.tsx contains `createFileRoute('/_authenticated/cash/')`
    - website/src/components/layout/data/sidebar-data.ts contains `url: '/cash'`
    - website/src/components/layout/data/sidebar-data.ts does NOT contain `disabled: true` for any item in the Billing group
  </acceptance_criteria>
  <done>Cash route registered at /cash, all three Billing sidebar items enabled</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Cash feature directory at `website/src/features/billing/cash/` with all components
- Route at `/_authenticated/cash/`
- Sidebar Cash link enabled and navigates to /cash
</verification>

<success_criteria>
- Admin can navigate to /cash from sidebar
- Petty cash balance card displayed above table
- Cash entries table with faceted status/type filters
- Inline approve button works on pending entries (no dialog)
- Reject opens dialog with reason textarea
- Create dialog has full form with Zod validation
- Top-up dialog adds to petty cash balance
</success_criteria>

<output>
After completion, create `.planning/phases/04-billing-payments/04-04-SUMMARY.md`
</output>
