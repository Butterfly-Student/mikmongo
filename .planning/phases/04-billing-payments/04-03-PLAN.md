---
phase: 04-billing-payments
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - website/src/features/billing/payments/data/schema.ts
  - website/src/features/billing/payments/data/columns.tsx
  - website/src/features/billing/payments/components/payment-table.tsx
  - website/src/features/billing/payments/components/payment-action-menu.tsx
  - website/src/features/billing/payments/components/confirm-payment-dialog.tsx
  - website/src/features/billing/payments/components/reject-payment-dialog.tsx
  - website/src/features/billing/payments/components/refund-payment-dialog.tsx
  - website/src/features/billing/payments/components/date-range-filter.tsx
  - website/src/features/billing/payments/index.tsx
  - website/src/routes/_authenticated/payments/index.tsx
  - website/src/components/layout/data/sidebar-data.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]

must_haves:
  truths:
    - "Admin can view all payments with faceted filters for method, status, and date range"
    - "Admin can confirm pending payments via a confirmation dialog"
    - "Admin can reject pending payments with a required reason field"
    - "Admin can refund confirmed payments with amount and reason"
    - "Admin can initiate gateway payment that opens payment URL in new tab"
  artifacts:
    - path: "website/src/features/billing/payments/index.tsx"
      provides: "Payment management page"
      contains: "Riwayat Pembayaran"
    - path: "website/src/features/billing/payments/components/payment-action-menu.tsx"
      provides: "Row action dropdown with conditional options"
      contains: "Konfirmasi Pembayaran"
    - path: "website/src/features/billing/payments/components/reject-payment-dialog.tsx"
      provides: "Rejection dialog with reason textarea"
      contains: "Tolak Pembayaran"
    - path: "website/src/features/billing/payments/components/refund-payment-dialog.tsx"
      provides: "Refund dialog with amount and confirmation"
      contains: "Kembalikan Dana"
    - path: "website/src/routes/_authenticated/payments/index.tsx"
      provides: "Route for /payments"
      contains: "createFileRoute"
  key_links:
    - from: "website/src/features/billing/payments/index.tsx"
      to: "website/src/hooks/use-payments.ts"
      via: "usePayments hook"
      pattern: "import.*usePayments.*from.*@/hooks/use-payments"
    - from: "website/src/features/billing/payments/components/payment-action-menu.tsx"
      to: "website/src/hooks/use-payments.ts"
      via: "mutation hooks for actions"
      pattern: "import.*useConfirmPayment|useRejectPayment|useRefundPayment|useInitiateGatewayPayment"
---

<objective>
Build the admin payment management page with DataTable, faceted filters (method, status, date range), and action dialogs for confirm/reject/refund/gateway.

Purpose: Admin can verify, approve, reject, and refund payments -- the payment workflow requirement.
Output: Complete payment page at `/payments` with all action dialogs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-billing-payments/04-CONTEXT.md
@.planning/phases/04-billing-payments/04-UI-SPEC.md
@.planning/phases/04-billing-payments/04-01-SUMMARY.md

<interfaces>
From website/src/lib/schemas/billing.ts:
```typescript
export type PaymentResponse = z.infer<typeof paymentResponseSchema>
// status: 'pending' | 'confirmed' | 'rejected' | 'refunded'
// payment_method: 'cash' | 'bank_transfer' | 'e-wallet' | 'credit_card' | 'debit_card' | 'check' | 'qris' | 'gateway'
export const rejectPaymentSchema // { reason: string }
export const refundPaymentSchema // { amount: number, reason: string }
```

From website/src/hooks/use-payments.ts:
```typescript
export function usePayments(limit?: number, offset?: number)
export function useConfirmPayment()
export function useRejectPayment()   // mutationFn: { id, reason }
export function useRefundPayment()   // mutationFn: { id, amount, reason }
export function useInitiateGatewayPayment() // mutationFn: { id, gateway }, opens new tab
```

From website/src/components/confirm-dialog.tsx:
```typescript
export function ConfirmDialog(props: { open, onOpenChange, title, desc, confirmText, cancelBtnText, destructive, handleConfirm, isLoading, children })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payment feature module (schema, columns, table, action menu, dialogs, date range filter)</name>
  <files>
    website/src/features/billing/payments/data/schema.ts,
    website/src/features/billing/payments/data/columns.tsx,
    website/src/features/billing/payments/components/payment-table.tsx,
    website/src/features/billing/payments/components/payment-action-menu.tsx,
    website/src/features/billing/payments/components/confirm-payment-dialog.tsx,
    website/src/features/billing/payments/components/reject-payment-dialog.tsx,
    website/src/features/billing/payments/components/refund-payment-dialog.tsx,
    website/src/features/billing/payments/components/date-range-filter.tsx,
    website/src/features/billing/payments/index.tsx
  </files>
  <read_first>
    website/src/features/billing/invoices/data/schema.ts,
    website/src/features/billing/invoices/data/columns.tsx,
    website/src/features/billing/invoices/components/invoice-table.tsx,
    website/src/features/billing/invoices/index.tsx,
    website/src/features/subscriptions/components/subscription-table.tsx,
    website/src/components/confirm-dialog.tsx,
    website/src/components/data-table/data-table-toolbar.tsx,
    website/src/components/data-table/data-table-faceted-filter.tsx,
    website/src/components/ui/popover.tsx,
    website/src/lib/schemas/billing.ts,
    website/src/hooks/use-payments.ts
  </read_first>
  <action>
**Per D-03:** All payment actions use confirmation dialogs. **Per D-04:** Faceted filters for method and status plus date range filter. **Per D-05:** Gateway payment URL opens in new browser tab.

**1. `features/billing/payments/data/schema.ts`:**
Re-export `PaymentResponse` from `@/lib/schemas/billing`. Define:
- `paymentStatuses`: `[{ value: 'pending', label: 'Menunggu Konfirmasi', className: 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900 dark:text-yellow-300' }, { value: 'confirmed', label: 'Dikonfirmasi', className: 'bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300' }, { value: 'rejected', label: 'Ditolak', className: 'bg-red-100 text-red-700 dark:bg-red-900 dark:text-red-300' }, { value: 'refunded', label: 'Dikembalikan', className: 'bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300' }]`
- `paymentMethods`: `[{ value: 'cash', label: 'Cash' }, { value: 'bank_transfer', label: 'Transfer Bank' }, { value: 'e-wallet', label: 'E-Wallet' }, { value: 'credit_card', label: 'Kartu Kredit' }, { value: 'debit_card', label: 'Kartu Debit' }, { value: 'qris', label: 'QRIS' }, { value: 'gateway', label: 'Gateway' }]`

**2. `features/billing/payments/data/columns.tsx`:**
`ColumnDef<PaymentResponse>[]`:
- `payment_number`: header "No. Referensi" (sortable), `font-mono text-xs`
- `invoice_number`: header "No. Tagihan" (not sortable) -- rendered from the payment data if available, or show "-"
- `payment_method`: header "Metode" (not sortable), capitalize label from `paymentMethods` lookup
- `amount`: header "Jumlah" (sortable), right-aligned, `Rp ${amount.toLocaleString('id-ID')}`
- `payment_date`: header "Tanggal" (sortable), `dd/MM/yyyy`
- `status`: header "Status", Badge with className from `paymentStatuses`
- `actions`: header "Aksi", renders `PaymentActionMenu` (receives row.original as `payment` prop)

**3. `features/billing/payments/components/date-range-filter.tsx`:**
Component with Popover containing two date inputs:
- Props: `{ dateFrom: string; dateTo: string; onDateFromChange: (val: string) => void; onDateToChange: (val: string) => void }`
- Trigger button labeled "Tanggal Pembayaran" (shows date range if set, or label if not)
- Inside popover: two `<Input type="date" />` fields labeled "Dari" and "Sampai"
- Reset button to clear both dates

**4. `features/billing/payments/components/payment-action-menu.tsx`:**
Props: `{ payment: PaymentResponse; onConfirm: (payment: PaymentResponse) => void; onReject: (payment: PaymentResponse) => void; onRefund: (payment: PaymentResponse) => void; onGateway: (payment: PaymentResponse) => void }`
Uses DropdownMenu:
- Trigger: `...` MoreHorizontal icon button
- Conditional items based on payment.status:
  - `pending`: "Konfirmasi Pembayaran" + "Tolak Pembayaran"
  - `confirmed`: "Kembalikan Dana"
- If payment.payment_method === 'gateway': "Buka Halaman Pembayaran" (always visible)

**5. `features/billing/payments/components/confirm-payment-dialog.tsx`:**
Uses ConfirmDialog:
- title: "Konfirmasi Pembayaran?"
- desc: "Pembayaran ini akan ditandai sebagai dikonfirmasi. Tindakan ini tidak dapat dibatalkan."
- confirmText: "Konfirmasi"
- cancelBtnText: "Batal"
- destructive: false
- handleConfirm calls `useConfirmPayment().mutate(payment.id)`, closes dialog on success

**6. `features/billing/payments/components/reject-payment-dialog.tsx`:**
Custom dialog (not ConfirmDialog, since it needs a Textarea child):
Uses AlertDialog pattern with:
- title: "Tolak Pembayaran?"
- desc: "Berikan alasan penolakan agar pelanggan dapat mengirimkan ulang bukti yang benar."
- Textarea field for reason, label "Alasan", placeholder "Masukkan alasan penolakan...", required
- "Batal" (ghost) + "Tolak" (destructive) buttons
- handleConfirm calls `useRejectPayment().mutate({ id: payment.id, reason })`
- Disable submit if reason is empty

**7. `features/billing/payments/components/refund-payment-dialog.tsx`:**
Uses ConfirmDialog:
- title: "Kembalikan Dana?"
- desc: `"Dana sebesar Rp ${payment.amount.toLocaleString('id-ID')} akan dikembalikan kepada pelanggan. Ini tidak dapat dibatalkan."`
- confirmText: "Kembalikan Dana"
- cancelBtnText: "Batal"
- destructive: true
- handleConfirm calls `useRefundPayment().mutate({ id: payment.id, amount: payment.amount, reason: 'Admin refund' })`

**8. `features/billing/payments/components/payment-table.tsx`:**
DataTable wrapper:
- Uses columns from columns.tsx
- DataTableToolbar with:
  - Search input placeholder "Cari nomor referensi atau tagihan..."
  - DataTableFacetedFilter for "payment_method" using `paymentMethods`
  - DataTableFacetedFilter for "status" using `paymentStatuses`
  - DateRangeFilter component

**9. `features/billing/payments/index.tsx`:**
Main page:
- Page header: title "Riwayat Pembayaran", subtitle "Konfirmasi, tolak, atau kembalikan dana pembayaran pelanggan"
- Uses `usePayments()` hook
- Loading: skeleton rows (6 rows, animate-pulse)
- Empty: "Tidak ada pembayaran ditemukan." with receipt icon (Receipt from lucide-react)
- Manages dialog state: `selectedPayment`, `confirmOpen`, `rejectOpen`, `refundOpen`
- PaymentActionMenu callbacks set selectedPayment and open appropriate dialog
- Gateway action calls `useInitiateGatewayPayment().mutate({ id: payment.id, gateway: 'xendit' })` directly (hook handles window.open and toast)
- Renders all three dialogs
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - website/src/features/billing/payments/index.tsx contains `Riwayat Pembayaran`
    - website/src/features/billing/payments/index.tsx contains `usePayments`
    - website/src/features/billing/payments/components/payment-action-menu.tsx contains `Konfirmasi Pembayaran`
    - website/src/features/billing/payments/components/payment-action-menu.tsx contains `Tolak Pembayaran`
    - website/src/features/billing/payments/components/payment-action-menu.tsx contains `Kembalikan Dana`
    - website/src/features/billing/payments/components/payment-action-menu.tsx contains `Buka Halaman Pembayaran`
    - website/src/features/billing/payments/components/reject-payment-dialog.tsx contains `Textarea`
    - website/src/features/billing/payments/components/reject-payment-dialog.tsx contains `useRejectPayment`
    - website/src/features/billing/payments/components/refund-payment-dialog.tsx contains `destructive`
    - website/src/features/billing/payments/data/schema.ts contains `paymentStatuses`
    - website/src/features/billing/payments/data/schema.ts contains `paymentMethods`
    - website/src/features/billing/payments/components/date-range-filter.tsx contains `Tanggal Pembayaran`
  </acceptance_criteria>
  <done>Payment page shows DataTable with method/status faceted filters and date range filter. Confirm, reject (with reason textarea), refund, and gateway actions work through dialogs. All Indonesian labels match UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Create payment route and enable sidebar nav</name>
  <files>
    website/src/routes/_authenticated/payments/index.tsx,
    website/src/components/layout/data/sidebar-data.ts
  </files>
  <read_first>
    website/src/routes/_authenticated/invoices/index.tsx,
    website/src/components/layout/data/sidebar-data.ts
  </read_first>
  <action>
**1. Create `website/src/routes/_authenticated/payments/index.tsx`:**
```typescript
import { createFileRoute } from '@tanstack/react-router'
import PaymentsPage from '@/features/billing/payments'

export const Route = createFileRoute('/_authenticated/payments/')({
  component: PaymentsPage,
})
```

**2. Update `website/src/components/layout/data/sidebar-data.ts`:**
In the "Billing" nav group, change the Payments item:
- `url: '#'` -> `url: '/payments'`
- Remove `disabled: true`
Keep Cash item still disabled (enabled in Plan 04).
  </action>
  <verify>
    <automated>cd /home/butterfly-student/Programing/Mikrotik/mikmongo-fully/website && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - website/src/routes/_authenticated/payments/index.tsx contains `createFileRoute('/_authenticated/payments/')`
    - website/src/components/layout/data/sidebar-data.ts contains `url: '/payments'` (for Payments item)
    - website/src/components/layout/data/sidebar-data.ts does NOT contain `disabled: true` adjacent to the Payments item
  </acceptance_criteria>
  <done>Payment route registered at /payments, sidebar Payments link enabled</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Payment feature directory at `website/src/features/billing/payments/` with all components
- Route at `/_authenticated/payments/`
- Sidebar Payments link navigates to /payments
</verification>

<success_criteria>
- Admin can navigate to /payments from sidebar
- Payment list shows with DataTable, search, method/status filters, date range filter
- Conditional action menu shows correct options per payment status
- Confirm/reject/refund dialogs work with correct mutation hooks
- Gateway initiation opens new tab
- All Indonesian labels match UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/04-billing-payments/04-03-SUMMARY.md`
</output>
