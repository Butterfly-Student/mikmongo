# Plan 01-01: Project Scaffold

**Phase:** 1 — Foundation & Auth
**Goal:** Bootstrap the complete `dashboard/` React application with Vite 6, React 19, TypeScript, TanStack Router v1 (file-based), TanStack Query v5, Shadcn/UI, Tailwind v4, and all supporting libraries. Every subsequent plan in Phase 1 builds on top of this scaffold.
**Requires:** Nothing
**Produces:** A runnable `npm run dev` in `dashboard/` with route tree generation active, Shadcn/UI initialized, Tailwind v4 CSS configured with OKLCH design tokens, dark-mode FOUC prevention, correct directory structure, and stub files for every layer.

---

## Pre-conditions

- [ ] Existing Go repo at `/home/butterfly-student/Programing/Mikrotik/mikmongo`
- [ ] Node.js 20+ installed (`node --version`)
- [ ] `npm` 10+ available (`npm --version`)
- [ ] No existing `dashboard/` directory (clean install)

---

## Tasks

### Task 1: Create Vite + React 19 + TypeScript project

**Command/Action:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo

# Scaffold inside dashboard/ using official Vite react-ts template
npm create vite@latest dashboard -- --template react-ts

cd dashboard

# Verify Node/npm versions are compatible
node --version
npm --version
```

**Expected result:** `dashboard/` directory created with `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `src/main.tsx`, `src/App.tsx`, `index.html`.

**Verify:**

```bash
ls /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard/
# Expected: index.html  package.json  public/  src/  tsconfig.app.json  tsconfig.json  vite.config.ts
```

---

### Task 2: Install all runtime and dev dependencies

**Command/Action:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard

# Core runtime dependencies
npm install \
  react@^19 react-dom@^19 \
  @tanstack/react-router@^1 \
  @tanstack/react-query@^5 \
  @tanstack/react-query-devtools@^5 \
  @tanstack/react-table@^8 \
  @tanstack/react-virtual@^3 \
  @tanstack/react-form@^1 \
  zustand@^5 \
  axios@^1 \
  zod@^3 \
  sonner@^2 \
  date-fns@^3 \
  tw-animate-css

# Dev dependencies
npm install -D \
  @tanstack/router-plugin@^1 \
  @tanstack/router-devtools \
  @vitejs/plugin-react@^4 \
  vite@^6 \
  typescript@^5 \
  tailwindcss@latest \
  @tailwindcss/vite \
  @types/node \
  @types/react@^19 \
  @types/react-dom@^19

# Icon library — required by Sidebar, Topbar, login pages, overview page
npm install lucide-react
```

**Expected result:** All packages resolved and installed without peer dependency conflicts. `node_modules/` populated. `package.json` reflects all dependencies.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
# Check critical packages installed at correct major versions
node -e "const p = require('./package.json'); console.log('react:', p.dependencies.react, 'router:', p.dependencies['@tanstack/react-router'], 'zustand:', p.dependencies.zustand)"
```

---

### Task 3: Configure `vite.config.ts`

Replace the scaffolded `vite.config.ts` with the full configuration: TanStack Router plugin (MUST be first), React plugin, Tailwind v4 vite plugin, `@/` path alias, and dev proxy for all three API namespaces.

**Command/Action:**

Write the following content to `dashboard/vite.config.ts`:

```typescript
// dashboard/vite.config.ts
import path from "path"
import { defineConfig } from "vite"
import react from "@vitejs/plugin-react"
import tailwindcss from "@tailwindcss/vite"
import { TanStackRouterVite } from "@tanstack/router-plugin/vite"

export default defineConfig({
  plugins: [
    // CRITICAL: TanStackRouterVite MUST be first — it scans src/routes/ before React transforms
    TanStackRouterVite({ target: "react", autoCodeSplitting: true }),
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  server: {
    port: 5173,
    proxy: {
      "/api": {
        target: "http://localhost:8080",
        changeOrigin: true,
      },
      "/agent-portal": {
        target: "http://localhost:8080",
        changeOrigin: true,
      },
      "/portal": {
        target: "http://localhost:8080",
        changeOrigin: true,
      },
    },
  },
})
```

**Expected result:** `vite.config.ts` contains all three plugins in the correct order, path alias, and proxy rules.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
grep -n "TanStackRouterVite\|tailwindcss\|react()" vite.config.ts
# TanStackRouterVite must appear on a lower line number than react()
```

---

### Task 4: Configure `tsconfig.json` path aliases

Update `tsconfig.app.json` (the one Vite uses for compilation) to include the `@/` path alias. Also update `tsconfig.json` to reference `tsconfig.app.json`.

**Command/Action:**

Write the following content to `dashboard/tsconfig.app.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

Write the following content to `dashboard/tsconfig.json`:

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" }
  ]
}
```

**Expected result:** TypeScript compiler resolves `@/` imports to `./src/`.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
grep -A 3 '"paths"' tsconfig.app.json
# Should show "@/*": ["./src/*"]
```

---

### Task 5: Create the full directory structure and stub files

Create all directories and stub files. Files are stubs only — no business logic. Logic comes in Plans 01-02 and 01-03.

**Command/Action:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard/src

# Create all directories
mkdir -p \
  routes/_admin \
  routes/agent/_agentAuth \
  routes/customer/_customerAuth \
  components/ui \
  components/layout \
  components/shared \
  lib/axios \
  store/slices \
  api \
  hooks \
  styles
```

Create stub file `src/routes/__root.tsx`:

```typescript
// src/routes/__root.tsx
// Root route — provides typed router context for all portals.
// Full implementation: Plan 01-02 (auth store wired here).
import { createRootRouteWithContext, Outlet } from "@tanstack/react-router"
import { QueryClient } from "@tanstack/react-query"

export interface RouterContext {
  adminAuth: {
    isAuthenticated: boolean
    role: string | null
    accessToken: string | null
  }
  agentAuth: {
    isAuthenticated: boolean
    accessToken: string | null
  }
  customerAuth: {
    isAuthenticated: boolean
    accessToken: string | null
  }
  queryClient: QueryClient
}

export const Route = createRootRouteWithContext<RouterContext>()({
  component: () => <Outlet />,
})
```

Create stub file `src/routes/index.tsx`:

```typescript
// src/routes/index.tsx
// Redirects root path to /dashboard (admin default landing).
import { createFileRoute, redirect } from "@tanstack/react-router"

export const Route = createFileRoute("/")({
  beforeLoad: () => {
    throw redirect({ to: "/dashboard" })
  },
})
```

Create stub file `src/routes/login.tsx`:

```typescript
// src/routes/login.tsx — admin login page (Plan 01-02 adds form logic)
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/login")({
  component: LoginPage,
})

function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Admin Login — coming in Plan 01-02</p>
    </div>
  )
}
```

Create stub file `src/routes/_admin/route.tsx`:

```typescript
// src/routes/_admin/route.tsx
// Pathless layout: wraps all admin routes with auth guard + AppShell.
// Auth guard implementation: Plan 01-02. AppShell: Plan 01-03.
import { createFileRoute, Outlet } from "@tanstack/react-router"

export const Route = createFileRoute("/_admin")({
  component: () => (
    <div>
      <Outlet />
    </div>
  ),
})
```

Create stub file `src/routes/_admin/dashboard.tsx`:

```typescript
// src/routes/_admin/dashboard.tsx — renders at URL: /dashboard
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/_admin/dashboard")({
  component: DashboardPage,
})

function DashboardPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="text-muted-foreground mt-2">Overview — coming in Plan 01-03</p>
    </div>
  )
}
```

Create stub file `src/routes/agent/login.tsx`:

```typescript
// src/routes/agent/login.tsx — agent portal login at /agent/login
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/agent/login")({
  component: AgentLoginPage,
})

function AgentLoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Agent Login — coming in Plan 01-02</p>
    </div>
  )
}
```

Create stub file `src/routes/agent/_agentAuth/route.tsx`:

```typescript
// src/routes/agent/_agentAuth/route.tsx
// Pathless layout: wraps all agent-portal routes. Auth guard in Plan 01-02.
import { createFileRoute, Outlet } from "@tanstack/react-router"

export const Route = createFileRoute("/agent/_agentAuth")({
  component: () => <Outlet />,
})
```

Create stub file `src/routes/agent/_agentAuth/dashboard.tsx`:

```typescript
// src/routes/agent/_agentAuth/dashboard.tsx — renders at /agent/dashboard
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/agent/_agentAuth/dashboard")({
  component: AgentDashboardPage,
})

function AgentDashboardPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Agent Portal</h1>
      <p className="text-muted-foreground mt-2">Coming in Phase 5</p>
    </div>
  )
}
```

Create stub file `src/routes/customer/login.tsx`:

```typescript
// src/routes/customer/login.tsx — customer portal login at /customer/login
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/customer/login")({
  component: CustomerLoginPage,
})

function CustomerLoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Customer Login — coming in Plan 01-02</p>
    </div>
  )
}
```

Create stub file `src/routes/customer/_customerAuth/route.tsx`:

```typescript
// src/routes/customer/_customerAuth/route.tsx
// Pathless layout: wraps all customer-portal routes. Auth guard in Plan 01-02.
import { createFileRoute, Outlet } from "@tanstack/react-router"

export const Route = createFileRoute("/customer/_customerAuth")({
  component: () => <Outlet />,
})
```

Create stub file `src/routes/customer/_customerAuth/dashboard.tsx`:

```typescript
// src/routes/customer/_customerAuth/dashboard.tsx — renders at /customer/dashboard
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/customer/_customerAuth/dashboard")({
  component: CustomerDashboardPage,
})

function CustomerDashboardPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Customer Portal</h1>
      <p className="text-muted-foreground mt-2">Coming in Phase 5</p>
    </div>
  )
}
```

Create stub `src/lib/utils.ts` (required by Shadcn/UI components):

```typescript
// src/lib/utils.ts — cn() helper, generated/used by Shadcn/UI
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Create stub `src/store/index.ts`:

```typescript
// src/store/index.ts — Zustand store (full implementation in Plan 01-02)
export {}
```

Create stub `src/api/types.ts`:

```typescript
// src/api/types.ts — Zod schemas for API responses (expanded in Plan 01-02)
export {}
```

Create stub `src/hooks/useTheme.ts`:

```typescript
// src/hooks/useTheme.ts — dark/light mode toggle (Plan 01-03 uses this)
import { useEffect, useState } from "react"

type Theme = "dark" | "light" | "system"

export function useTheme() {
  const [theme, setTheme] = useState<Theme>(() => {
    return (localStorage.getItem("theme") as Theme) ?? "system"
  })

  useEffect(() => {
    const root = document.documentElement
    const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches

    if (theme === "dark" || (theme === "system" && prefersDark)) {
      root.classList.add("dark")
    } else {
      root.classList.remove("dark")
    }

    if (theme === "system") {
      localStorage.removeItem("theme")
    } else {
      localStorage.setItem("theme", theme)
    }
  }, [theme])

  return { theme, setTheme }
}
```

**Expected result:** All directories and stub files exist. No logic — just exports and placeholder JSX.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
find src/routes -name "*.tsx" | sort
# Expected: __root.tsx, index.tsx, login.tsx, _admin/route.tsx, _admin/dashboard.tsx,
#           agent/login.tsx, agent/_agentAuth/route.tsx, agent/_agentAuth/dashboard.tsx,
#           customer/login.tsx, customer/_customerAuth/route.tsx, customer/_customerAuth/dashboard.tsx
```

---

### Task 6: Configure Tailwind v4 CSS and Shadcn/UI

**Step 6a: Run `npx shadcn@latest init`**

This must be run AFTER `tsconfig.json` has the `@/` alias (done in Task 4) so the CLI detects the correct component output path.

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard

# Interactive prompts — use these answers:
# Which style? → New York
# Which color? → Neutral  (or Zinc — both work with OKLCH tokens)
# Add CSS variables? → Yes
# Where is your global CSS? → src/styles/globals.css
# Configure aliases? → Yes (should auto-detect @/ from tsconfig)
npx shadcn@latest init
```

The CLI will:
- Detect Tailwind v4 from `@tailwindcss/vite` in `package.json`
- Generate `components.json`
- Create/update `src/styles/globals.css` with `@import "tailwindcss"`, `@import "tw-animate-css"`, `@custom-variant dark`, `@layer base` with OKLCH CSS variables, and `@theme inline {}` block
- Install `clsx` and `tailwind-merge` (required for the `cn()` helper)

**Step 6b: Add dark mode FOUC prevention to `index.html`**

Edit `dashboard/index.html` to add the inline theme script inside `<head>` BEFORE the stylesheet link. This script runs synchronously before paint and prevents the white flash when the OS is in dark mode.

The `<head>` section should look like:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script>
      (function () {
        var stored = localStorage.getItem("theme");
        var prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
        if (stored === "dark" || (!stored && prefersDark)) {
          document.documentElement.classList.add("dark");
        }
      })();
    </script>
    <title>MikMongo Dashboard</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 6c: Add one Shadcn/UI component to confirm setup works**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
npx shadcn@latest add button
```

This confirms the component generator works end-to-end and `src/components/ui/button.tsx` is placed correctly.

**Expected result:** `components.json` exists. `src/styles/globals.css` contains OKLCH CSS variables. `src/components/ui/button.tsx` exists. `index.html` has the FOUC-prevention script.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
# Shadcn config
test -f components.json && echo "components.json OK"
# CSS variables
grep -c "oklch" src/styles/globals.css
# Expected: 20+ matches
# Button component
test -f src/components/ui/button.tsx && echo "button.tsx OK"
# FOUC script
grep "prefers-color-scheme" index.html && echo "FOUC script OK"
```

---

### Task 7: Rewrite `src/main.tsx` with full provider setup

Replace the Vite-generated `src/main.tsx` with the correct entrypoint: QueryClientProvider wrapping the app, RouterProvider receiving context from Zustand auth state (stubs for now), React DevTools, and Sonner Toaster.

**Command/Action:**

Write the following content to `dashboard/src/main.tsx`:

```typescript
// src/main.tsx
// Application entrypoint — wires QueryClient + Router + auth context.
// Auth context hooks (useAdminAuthContext, etc.) are stubs here;
// Plan 01-02 fills them with real Zustand selectors.
import React from "react"
import ReactDOM from "react-dom/client"
import { RouterProvider, createRouter } from "@tanstack/react-router"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { ReactQueryDevtools } from "@tanstack/react-query-devtools"
import { Toaster } from "sonner"

// Route tree generated by TanStack Router Vite plugin
import { routeTree } from "./routeTree.gen"

// Global styles (Tailwind v4 + Shadcn/UI CSS variables)
import "./styles/globals.css"

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,   // 5 minutes
      gcTime: 1000 * 60 * 10,      // 10 minutes
      retry: 2,
      refetchOnWindowFocus: false,
    },
  },
})

const router = createRouter({
  routeTree,
  context: {
    // Stub context — Plan 01-02 replaces undefined! with real auth selectors
    adminAuth: { isAuthenticated: false, role: null, accessToken: null },
    agentAuth: { isAuthenticated: false, accessToken: null },
    customerAuth: { isAuthenticated: false, accessToken: null },
    queryClient,
  },
})

declare module "@tanstack/react-router" {
  interface Register {
    router: typeof router
  }
}

function App() {
  return (
    <>
      <RouterProvider router={router} />
      <Toaster position="top-right" richColors />
      {import.meta.env.DEV && <ReactQueryDevtools initialIsOpen={false} />}
    </>
  )
}

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
    </QueryClientProvider>
  </React.StrictMode>
)
```

Delete the Vite-generated `src/App.tsx` and `src/App.css` since they are replaced by the router-based structure:

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
rm -f src/App.tsx src/App.css
```

**Expected result:** `src/main.tsx` uses RouterProvider, QueryClientProvider, and Sonner Toaster. No reference to the old `App` component.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
grep -n "RouterProvider\|QueryClientProvider\|Toaster" src/main.tsx
# All three should appear
test ! -f src/App.tsx && echo "App.tsx removed OK"
```

---

### Task 8: Add `.env.example` and verify `npm run dev` starts

**Command/Action:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard

# Create env example (actual .env is gitignored)
cat > .env.example << 'EOF'
# API base URL — Vite dev proxy rewrites these paths to localhost:8080
# In production, set this to the actual API host (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080
EOF

# Copy to .env for local development
cp .env.example .env
```

Add `dashboard/.env` to the project `.gitignore` (if not already covered):

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo
# Append dashboard env to root gitignore if not already there
grep -q "dashboard/.env$" .gitignore || echo "dashboard/.env" >> .gitignore
```

Run the dev server and confirm it starts without errors. Run in background and capture PID, then kill it:

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard

# Start dev server — should report "ready in Xms" with no errors
npm run dev &
DEV_PID=$!
sleep 5

# Check that the process is still running (not crashed)
if kill -0 $DEV_PID 2>/dev/null; then
  echo "Dev server running OK — PID $DEV_PID"
  kill $DEV_PID
else
  echo "FAIL: Dev server crashed — check output above"
  exit 1
fi
```

Run TypeScript type check (no emit):

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard
npx tsc --noEmit
```

**Expected result:** `npm run dev` outputs `VITE ready in Xms` and `Local: http://localhost:5173/`. TypeScript reports zero errors. `routeTree.gen.ts` is generated by the plugin.

**Verify:**

```bash
cd /home/butterfly-student/Programing/Mikrotik/mikmongo/dashboard

# routeTree.gen.ts must be generated by the plugin
test -f src/routeTree.gen.ts && echo "routeTree.gen.ts OK"

# TypeScript check
npx tsc --noEmit && echo "TypeScript OK"

# .env.example present
test -f .env.example && echo ".env.example OK"
```

---

## Verification

Run each check in order. All must pass before marking plan complete.

- [ ] `npm run dev` starts at `http://localhost:5173` with no errors in console
- [ ] `http://localhost:5173` loads a React app (root route redirects to `/dashboard`)
- [ ] `http://localhost:5173/dashboard` renders "Dashboard" heading
- [ ] `http://localhost:5173/login` renders "Admin Login" placeholder
- [ ] `http://localhost:5173/agent/login` renders "Agent Login" placeholder
- [ ] `http://localhost:5173/customer/login` renders "Customer Login" placeholder
- [ ] `npx tsc --noEmit` exits with zero errors
- [ ] `src/routeTree.gen.ts` exists and is non-empty
- [ ] `src/styles/globals.css` contains `@import "tailwindcss"` and `oklch` color tokens
- [ ] `src/components/ui/button.tsx` exists (Shadcn/UI working)
- [ ] `components.json` exists at `dashboard/components.json`
- [ ] `index.html` contains the FOUC-prevention inline script
- [ ] `dashboard/.env.example` committed, `dashboard/.env` gitignored

---

## Files Created/Modified

```
dashboard/
├── .env                          (local only, gitignored)
├── .env.example                  (committed)
├── components.json               (shadcn config, generated)
├── index.html                    (modified: FOUC script added, title updated)
├── package.json                  (generated + all deps installed)
├── tsconfig.json                 (modified: references tsconfig.app.json)
├── tsconfig.app.json             (modified: @/ path alias added)
├── vite.config.ts                (replaced: TanStack Router + Tailwind v4 + proxy)
└── src/
    ├── main.tsx                  (replaced: RouterProvider + QueryClient + Sonner)
    ├── routeTree.gen.ts          (auto-generated by plugin — DO NOT EDIT)
    ├── styles/
    │   └── globals.css           (generated by shadcn init: Tailwind v4 + OKLCH tokens)
    ├── routes/
    │   ├── __root.tsx            (RouterContext interface + createRootRouteWithContext)
    │   ├── index.tsx             (redirect / → /dashboard)
    │   ├── login.tsx             (admin login stub)
    │   ├── _admin/
    │   │   ├── route.tsx         (pathless layout stub — auth guard in Plan 01-02)
    │   │   └── dashboard.tsx     (renders at /dashboard — overview in Plan 01-03)
    │   ├── agent/
    │   │   ├── login.tsx         (agent login stub at /agent/login)
    │   │   └── _agentAuth/
    │   │       ├── route.tsx     (agent pathless layout stub)
    │   │       └── dashboard.tsx (renders at /agent/dashboard)
    │   └── customer/
    │       ├── login.tsx         (customer login stub at /customer/login)
    │       └── _customerAuth/
    │           ├── route.tsx     (customer pathless layout stub)
    │           └── dashboard.tsx (renders at /customer/dashboard)
    ├── components/
    │   ├── ui/
    │   │   └── button.tsx        (shadcn Button — confirms generator works)
    │   ├── layout/               (directory only — AppShell etc. in Plan 01-03)
    │   └── shared/               (directory only)
    ├── lib/
    │   ├── axios/                (directory only — clients in Plan 01-02)
    │   └── utils.ts              (cn() helper for Shadcn)
    ├── store/
    │   ├── index.ts              (stub — Zustand store in Plan 01-02)
    │   └── slices/               (directory only)
    ├── api/
    │   └── types.ts              (stub — Zod schemas in Plan 01-02)
    └── hooks/
        └── useTheme.ts           (dark/light mode hook — used in Plan 01-03)
```

---

## Requirements Addressed

| Requirement | Status |
|-------------|--------|
| SETUP-01: React + Vite + TypeScript in `dashboard/` | Fully addressed — Task 1 |
| SETUP-02: TanStack Router v1 file-based, 3 route trees | Addressed — Tasks 3, 5 (stubs; guards in Plan 01-02) |
| SETUP-03: TanStack Query v5 with global QueryClient | Addressed — Task 7 (QueryClient in main.tsx) |
| SETUP-04: Shadcn/UI + Tailwind v4 with design tokens | Fully addressed — Task 6 |
| SETUP-05: Dark mode with system pref + toggle | Addressed — FOUC script (Task 6b), useTheme hook (Task 5) |

---

## What This Plan Does NOT Do

The following are intentionally deferred to their respective plans:

- **Auth store, login forms, RBAC guards** → Plan 01-02
- **AppShell, Sidebar, Topbar, mobile nav** → Plan 01-03
- **Axios instances with interceptors** → Plan 01-02
- **Zustand store slices** → Plan 01-02
- **Overview dashboard summary cards** → Plan 01-03
